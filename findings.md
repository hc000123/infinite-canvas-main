# 素材库专项发现记录

## 已确认基线

- 素材和文件主体保存在浏览器本地：素材列表由 Zustand persist 保存，业务大数据由 localForage / IndexedDB 保存。
- 已完成但尚未发布的修复包括：Store 恢复写入门控、画布生成归档等待、持久化 Blob 下载、旧/新工作流编号的规范视图去重。
- 素材修复已纳入当前发布历史，后续审计必须在其上增量进行，不能回滚。
- 当前 14 个素材相关测试文件共 62 项用例全部通过，可作为后续 RED/GREEN 回归基线。

## 待审计问题

- 所有素材写入入口是否统一走安全写入路径。
- `addAsset` 与 `addAssetOnce` 的职责是否导致文本素材绕过去重或恢复门控。
- 规范视图的虚拟合并是否会影响编辑、版本、引用和删除。
- 导入素材包是否会保留不安全或冲突的元数据。
- Blob 清理是否可能在别名/版本仍被引用时误删文件。
- 页面查询是否重复遍历素材列表，是否存在可安全收口的热点。
- 页面首屏是否堆叠过多统计、筛选和批量动作，标签/卡片层级是否重复。
- 画布生成、项目工作台、生产包/素材拆解等入口是否统一写入，素材回取是否显示原始重复记录。

## 已确认问题

- 写入入口并未完全统一：大多数图片/视频路径使用异步 `addAssetOnce`，但提示词库、素材编辑器文本保存、旧素材库入口和工作流素材面板仍直接调用同步 `addAsset`。
- `addAssetOnce` 对文本素材直接退回 `addAsset`，因此具备同一工作流身份的文本记录不会在写入层去重；目前只在“我的素材”查询视图中隐藏重复项，原始 Store 仍可能持续膨胀。
- 画布生成、画布节点归档、图片/视频页和集数工作台的媒体写入大多已使用 `addAssetOnce`，跨流程主干方向一致；风险集中在文本工作流素材和水合前同步写入。
- Store 的 persist 配置没有自定义恢复合并逻辑：同步 `addAsset`、`updateAsset` 或 `removeAsset` 若发生在异步恢复完成前，默认恢复可能用旧持久化快照覆盖当前内存写入。现有门控只保护 `addAssetOnce`，不能保护这些同步入口。
- 页面头部同时展示“总数/当前匹配/已选择”三块指标，结果工具栏又重复展示当前筛选和选择数量；首屏信息重复并挤压真实内容。
- 页面头部常驻“返回、导出全部、导入、新增”四个按钮；其中“返回”属于导航，“导出全部”属于低频危险范围动作，与高频导入/新增并排导致操作优先级不清。
- 规范化重复记录目前只在“我的素材”页面查询中应用；画布素材选择器、视频工作流页、工作台抽屉等十余个消费者仍直接读取原始 Store，因此同一批隐藏别名仍可能在跨流程取用时重新出现。
- 查询层保留了 `page/pageSize` 状态和分页 UI 接口，但 `assetPaginationEnabled` 被固定为 `false`，`visibleAssets` 直接等于全部筛选结果；长列表会一次性渲染所有分组和素材行。
- 结果区存在“项目总计标签 → 类型快捷标签 → 类型折叠标题 → 卡片状态标签”的多层重复计数，工作流素材尤为密集，是当前界面清洁度不足的主要来源。
- 画布的“选择素材”弹窗已自带分页，但“我的素材”标签直接使用原始 Store，因此重复工作流素材会在画布取用时再次出现；这是最明确的跨流程不一致入口。
- 筛选区首屏常驻类型、项目、集数、文件夹、生成共 4–5 个带边框区块，同时搜索栏再次展示匹配数/选择数；常用筛选与高级筛选没有分层，是页面显得拥挤的第二个主因。
- 工作流素材面板会先读取原始 Store 做映射，再对新文本素材使用同步 `addAsset`；这条路径同时暴露于恢复覆盖和原始记录膨胀风险。
- Blob 清理会递归扫描完整素材/画布对象，能够识别 `assetVersions` 内的 storageKey，因此当前版本历史不会因删除其他素材而被误清理。
- 素材包导出只收集每个素材“当前 data.storageKey”的文件，虽然 JSON 会带上 `assetVersions`，历史版本对应的 Blob 并未写入压缩包；跨浏览器导入后历史版本可能存在记录但无法预览、恢复或下载。
- 素材包导入先按包内原 storageKey 写入本地，再创建新素材；没有对 storageKey 做重新分配。导入同源旧包时存在覆盖本地同键 Blob 的理论风险，也会留下未被最终素材引用的文件。
- 批量导入直接以 `addAssetOnce` 返回次数作为成功数量；重复媒体被归并到同一素材 ID 时仍会重复计数，提示与实际新增/更新条数可能不一致。
- 导入包现在为包内每个 Blob 分配新的本地 storageKey，并同步重写当前素材和历史版本引用，避免覆盖已有同键文件。
- 隔离环境实测确认默认页不再出现文件夹和生成来源区块，展开“更多筛选”后才显示；头部、筛选和结果区的重复计数已消除，控制台无错误。
- 已上线实例使用的是独立持久化账号数据，不能把容器初始环境变量视为当前登录凭据；专项验收改用临时数据库，未写入现有实例。

## 决策

- 正确性优先，同时采用“克制的工作台”界面方向：延续当前主题，突出搜索、筛选和核心批量动作，降低装饰和重复状态的视觉重量。
- 不引入与当前主题冲突的字体、硬编码颜色或重动画。
- 只自动合并能够用项目、集数、工作流名称和稳定逻辑编号共同确认的记录。
- 旧引用通过隐藏别名保留，用户明确删除规范组时才删除同组记录。
- Store 恢复采用“当前内存记录优先、持久化记录补齐”的按 ID 合并，只解决恢复期同步新增/同 ID 更新覆盖，不引入额外迁移或队列。
- 工作流写入去重只依据项目、集数、素材名和逻辑编号；普通手动文本仍允许同名并存。
- 界面保留搜索、类型、项目作为首屏主层级；集数、文件夹、生成与引用范围改为按需展开，高级条件启用时在入口提示。
