# 提示词五类业务分类发现记录

## 数据流

- 后台 `/admin/prompts` 与外侧 `/prompts` 原本已经共用 `/api/prompts` 和 `Prompt.Category`，不需要新增同步服务。
- 原问题是 repository 只暴露 `system` 一个分类，23 条种子也全部写入该值，导致前后台只能显示“系统”。
- `tags` 已是独立 JSON 数组，后台查询支持多标签组合，继续作为自由标签最合适。

## 实施结论

- `Prompt.Category` 直接固定为 `scene / prop / character / video / text`，避免新增重复字段。
- 场景种子归场景，角色/脸/手模板归角色，视频和文本按节点类型归类，其余通用图片资产与处理模板归道具。
- 启动补齐仅更新仍为 `system` 或空分类的内置种子，不覆盖标题、正文、标签、变量或管理员后续分类。
- 外侧左栏通过分类编码调用原有服务端筛选，后台新增/编辑后刷新即可看到同一记录。

## 运行态结论

- 后台页面已显示固定五类下拉和业务分类列，新建表单要求选择业务分类，并保留自由填写的自定义标签。
- 外侧 `/prompts` 与后台共用 `/api/prompts`；场景分类运行态读取到 8 条后台记录，证明分类查询链路已贯通。
- 当前 23 条内置种子均为图片相关模板，只分布于场景、道具、角色；视频、文本分类为空是现有数据结果，不是同步或筛选失败。管理员新增对应类别后会通过同一接口直接展示。
- Docker 容器运行健康。后端测试首次在沙箱内因 `httptest` 无权绑定回环端口失败，允许本机测试端口后以相同命令重跑并通过。
