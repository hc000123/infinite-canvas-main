# VT-20260812-DRILL-01 只读派单演练

## 派工单

- 目标：梳理画布装配层、hooks、components、stores 与 utils 的职责边界。
- 主责：01 产品与技术架构。
- 协作：02 画布前端。
- 验收：00 画布开发总控。
- 权限：只读，无文件所有权，不运行构建、测试、发布、推送或付费调用。

## 员工返回摘要

01 与 02 均基于当前代码独立确认：画布已经形成路由、装配页、hooks、components、stores、utils / services 的分层主干，整体依赖方向合理。

双方共同确认的审查信号：

- `canvas-client-page.tsx` 中仍有包含校验、节点构造、连线和多状态写入的完整业务流程。
- 部分 `utils` 文件包含 API、上传、store 写入或 UI 副作用，不符合纯计算边界。
- 展示组件中的跨 API、Workflow、Invocation / Artifact 编排应优先下沉到职责型 hook。

02 额外指出节点渲染期可能存在随节点和连线数量放大的重复派生成本；该项没有实际性能测量，只记录为待验证风险，不作为已确认缺陷。

## 总控结论

只沉淀双方一致且跨任务稳定的职责边界：

- `canvas-client-page.tsx` 是组合根，负责接线和最终布局。
- 页面高频编辑态与 `useCanvasStore` 持久化快照构成双层状态，保存动作是提交边界。
- `useCanvasWorkspaceStores` 是跨域 store 聚合适配层。
- utils 应保持纯计算和数据转换；副作用归入 services、hooks 或 components。

具体行号、当前越界实例和未测量性能风险不写入长期决策，后续立项时重新读取代码并验证。

## 演练结果

- 派单：成功。
- 并行只读协作：成功。
- 员工越权指挥：未发生。
- 文件修改：员工未修改；仅总控更新共享记忆。
- 构建 / 测试：未运行，符合本次权限。
- 制度修订：无需修订。
