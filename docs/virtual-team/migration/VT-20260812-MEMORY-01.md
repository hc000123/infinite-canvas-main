# VT-20260812-MEMORY-01 历史记忆迁移报告

## 结果

已将旧 Codex 对话、Git 历史、设计/实施文档和正式项目文档中的稳定知识，按职责写入七份岗位文件。迁移只处理团队记忆文档，没有修改产品代码，也没有运行构建、测试、发布、推送或付费调用。

## 审计范围

- 七名岗位员工分别执行最多三轮只读检索与当前事实复核。
- 重点覆盖架构演进、画布引用与持久化、后端会话和任务运行时、模型渠道与 Invocation、UI/UX、质量门禁、发布部署与数据恢复。
- 对话正文只用作线索；最终结论均以 `AGENTS.md`、当前代码、正式文档或已合并 Git 记录校正。

## 迁移分类

- 当前有效：写入各岗位“当前有效”，可指导后续审查与派工。
- 历史背景：只解释架构为何形成，不作为新实现要求。
- 已被替代：保留旧方案的退出边界，防止回退。
- 尚未验证：保留待决策或待真实验收事项，禁止当作事实引用。

## 岗位分配摘要

| 岗位 | 已分配的核心记忆 |
|---|---|
| 01 架构 | Workflow + Skill、Invocation / Artifact 真相、资产单一事实源、固定岗位 Agent 退出边界 |
| 02 画布前端 | 稳定引用、`inputOrder`、资产身份/名称/版本、缓存与回写、双层状态、性能与连续性引用 |
| 03 后端数据 | 分层、单有效会话、密钥边界、Invocation/队列幂等、异步任务映射、存储容量 |
| 04 AI Runtime | Skill 冻结、Artifact/质量/审核/Apply、渠道适配、费用确认、异步重试与参考图角色 |
| 05 UI/UX | 统一主题、低视觉重量、安全视口、滚动所有权、工作台阅读流、资产状态和响应式 |
| 06 质量 | 证据等级、画布引用、运行时一致性、权限/会话/密钥、归档、运行态和付费禁区 |
| 07 发布运维 | 唯一发布清单、分层上线证据、版本/tag、单容器连续性、持久化、备份回滚和 Worker 健康 |

## 未写成事实的冲突

- `/workflow` 与 `/agent` 的 canonical URL：正式文档和当前路由不一致。
- `docs/canvas-node-manual.md` 的“尾帧作为下一镜首帧”：已被 `AGENTS.md` 和当前 `continuity_reference` 契约否定。
- 部分规划文档版本基线与当前版本记录漂移。
- 对象存储、容量告警、完整任务对账、无停机发布、真实多数据库迁移恢复尚无现行完整证据。

## 明确排除

未迁移密钥、令牌、账号、服务器地址、镜像 digest、备份路径、用户数据、任务 ID、大段日志、临时测试数字、旧版本号、一次性文件行号或仅由对话标题/摘要支持的结论。

## 后续使用规则

岗位文件提供检索方向和稳定审查规则，不替代重新读取当前代码。若岗位记忆与用户当前要求、`AGENTS.md` 或当前代码冲突，按团队事实优先级处理并向 00 总控报告。

## 接收确认

01–07 七名员工均已重新读取各自岗位文件、迁移报告和总控发现记录，并返回“已接收”。没有新增冲突；各岗位列出的生产环境、真实浏览器、高并发、供应商对账和文档入口等未决事项继续保持“尚未验证”。
