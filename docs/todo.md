# 迭代计划

本文档记录后续功能的整体规划、执行顺序和验收边界。当前工具已经从“画布生成工具”进入“AI 短剧创作工作台”阶段：项目是总入口，画布负责自由编排，剧本和分镜负责结构化生产，素材库负责资产沉淀，提示词仓库负责复用方法，已有 Agent 负责本地建议和动作预览。

已完成但仍需要真实页面确认的事项继续记录在 `docs/pending-test.md`。本文档只记录接下来要做什么、为什么做、先后顺序和不做什么。

## 当前基线

- 当前版本：`v0.2.41`。
- 已具备项目工作台、画布项目预设、剧本管理、分镜管理、设定库、提示词仓库、素材自动归档、素材去重、素材版本历史、素材版本引用锁定、生成队列、剪辑包导出、本地 Agent 工作台和 Agent Skill registry。
- 火山方舟 / Seedance 的本地直连边界已收口：本地直连只保留 OpenAI 兼容能力，火山方舟 / Seedance 企业能力走云端渠道；火山素材加白仍由后台系统设置维护。
- 当前核心短板不再是“缺入口”，而是生产链路的收口：项目首页缺少全局进度视图，素材引用总览仍需增强，后处理能力未接入，前台素材与后台任务账本尚未完全互查。

## 规划原则

- 项目优先：新能力优先挂在项目上下文下，避免继续形成分散工作台。
- 结果资产化：图片、视频、音频和后处理结果都要进入素材体系，并记录来源、提示词、模型、参数、任务和引用关系。
- 引用可控：画布、分镜、设定库默认锁定素材版本，不因素材更新自动改变旧项目结果。
- Agent 可确认：Agent 可以分析、建议、预览和生成动作草案，但不能绕过用户确认修改素材、画布、分镜、主版本或触发扣费生成。
- 云端后置但边界明确：本地能力先稳定数据模型；需要密钥、扣费、任务日志和多人共享的能力走后端云端渠道。
- 后处理不覆盖原视频：超分、字幕抹除、补帧、增强等结果都作为新素材版本或派生素材保存。
- 文档分层：路线图写在本文档；已实现待测写在 `docs/pending-test.md`；用户确认稳定后再沉淀到 `docs/features.md`。
- 后续执行指令必须标注模型推理程度：`中`、`高`、`超高`，用于控制额度消耗。

## 新版执行主线

### A. 当前能力收口

目标：先把 `v0.2.32` 已有能力变成稳定可用的生产底座，减少“功能做了但路径散、页面待测多、文档过长”的问题。

优先级：最高。后续新功能都依赖这条线的稳定性。

1. M7.2.3 素材版本批量更新与旧版本下载。（已实现，待页面验收）
2. M7.3 待验收清单治理。（已完成，待人工确认结构）
3. M7.4 项目级素材引用总览。（已实现，待页面验收）

### B. 短剧生产闭环

目标：让用户从“新建项目”开始，能清楚看到剧本、分镜、素材、生成、待验收视频和剪辑交接的状态，形成真正可执行的短剧生产流程。

优先级：高。完成后工具的主体验会从“功能集合”变成“项目工作流”。

1. M6.6 项目工作流首页增强。（已实现，待页面验收）
2. M6.6.1 一集一画布与剧本导入。（已实现，待页面验收）
3. M6.6.2 剧本资产拆解与资产图 Brief。（已实现，待页面验收）
4. M6.6.R1 代码结构收口。（已实现，待回归确认）
5. M6.6.3 分镜头表与生成镜头组。（已实现，待页面验收）
6. M6.7 生图 Brief 工作台。（已实现，待页面验收）
7. M6.7.R1 Brief 工作台结构收口。（已完成，待回归确认）
8. M6.7.1 Brief 接入生图与结果归档。（已实现，待页面验收）
9. M6.7.2 Brief 结果版本对比与主参考图强化。（已实现，待页面验收）
10. M6.7.3 Brief 导出为美术设定表 / 生图提示词表。（已实现，待页面验收）
11. M8 生成历史与任务日志打通。（已实现，待页面验收）
12. M8.R1 追溯链路结构收口与自查。（已实现，待回归确认）

### C. 云端资产与协作

目标：把项目共享资产库从本地 metadata 预留升级为真正可多人访问的云端资产库，并为后续多端同步做准备。

优先级：中。需要在本地素材、版本、引用关系稳定后推进。

1. M10.1 云端文件表与对象存储底座。
2. M10.2 项目共享资产库云端版。
3. M10.3 云同步与冲突策略。

## 近期执行顺序

1. M7.2.3 素材版本批量更新与旧版本下载。（已实现，待页面验收）
2. M7.3 待验收清单治理。（已完成，待人工确认结构）
3. M7.4 项目级素材引用总览。（已实现，待页面验收）
4. M6.6 项目工作流首页增强。（已实现，待页面验收）
5. M6.6.1 一集一画布与剧本导入。（已实现，待页面验收）
6. M6.6.2 剧本资产拆解与资产图 Brief。（已实现，待页面验收）
7. M6.6.R1 代码结构收口。（已实现，待回归确认）
8. M6.6.3 分镜头表与生成镜头组。（已实现，待页面验收）
9. M6.7 生图 Brief 工作台第一版。（已实现，待页面验收）
10. M6.7.R1 Brief 工作台结构收口。（已完成，待回归确认）
11. M6.7.1 Brief 接入生图与结果归档。（已实现，待页面验收）
12. M6.7.2 Brief 结果版本对比与主参考图强化。（已实现，待页面验收）
13. M6.7.3 Brief 导出为美术设定表 / 生图提示词表。（已实现，待页面验收）
14. M8 生成历史与任务日志打通。（已实现，待页面验收）
15. M8.R1 追溯链路结构收口与自查。（已实现，待回归确认）
15. M10.1 云端文件表与对象存储底座。
16. M10.2 项目共享资产库云端版。

## A 线：当前能力收口

### M7.2.3：素材版本批量更新与旧版本下载

目标：补齐 M7.2 剩余的本地版本管理能力，让用户能在项目范围内看清哪些地方引用了旧版本，并可按需更新。

状态：已实现，待页面验收。当前已支持旧版本下载、项目过期引用筛选、单项/批量更新到最新版，并通过 `previousVersions` 保留引用方更新历史；后续继续做项目级素材引用总览。

交付内容：

- 素材详情的版本历史支持旧版本单独下载。
- 项目素材视图新增“过期引用”筛选。
- 支持按画布、分镜、设定库查看引用了旧版本的对象。
- 支持单项更新到最新版，延续 M7.2.2 的引用方更新逻辑。
- 支持批量更新选中的过期引用。
- 批量更新前展示影响对象，确认后才写入引用方。
- 更新记录写入引用方 `previousVersions` 或等价追溯字段。

验收标准：

- 替换素材生成新版本后，项目内能看到所有旧版本引用。
- 批量更新只修改引用方记录，不修改素材本体。
- 用户能下载旧版本文件。
- 固定版本引用不会在未确认时自动变更。

不做事项：

- 不做云端版本表。
- 不做多人更新动态。
- 不做自动全项目静默更新。

### M7.3：待验收清单治理

目标：把 `docs/pending-test.md` 中长期堆积的历史验收项重新分层，减少后续判断成本。

状态：已完成，待人工确认结构。当前已将 `docs/pending-test.md` 分为“当前必须验收 / 回归保留 / 历史记录 / 已过时或待确认是否废弃”，并把最近 M7 素材资产中心能力整理到顶部验收清单；下一步继续执行 M7.4 项目级素材引用总览。

交付内容：

- 将待测项分为：必须验收、回归保留、历史记录、已过时。
- 顶部增加当前版本必须验收清单。
- 历史 M2 / M3 / M4 的长记录折叠为摘要，不再挤占当前版本视野。
- 每个必须验收项明确入口、操作、预期结果和失败影响。
- 建立“验收通过后迁移到 features”的规则说明。

验收标准：

- 打开 `docs/pending-test.md`，能在一分钟内看出当前最该测什么。
- 历史记录仍可追溯，但不会干扰当前版本判断。
- 后续每轮实现都能把可测试事项追加到对应分组。

不做事项：

- 不删除真实历史记录。
- 不把未验收能力写成正式已发布功能。

### M7.4：项目级素材引用总览

目标：让用户从项目维度看清素材被哪里使用，解决“素材多了以后不敢删、不敢替换、不知道影响哪里”的问题。

状态：已实现，待页面验收。当前项目详情页新增“素材引用”Tab，可按素材聚合画布、分镜、设定库、分镜结果和 generation metadata 引用，并支持类型、引用类型、版本状态和项目库状态筛选；后续继续推进项目工作流首页增强。

交付内容：

- 项目详情新增“素材引用”视图或入口。
- 按素材展示引用对象：画布节点、分镜条目、设定库、生成结果、剪辑包。
- 支持按素材类型、版本状态、分镜组、引用类型筛选。
- 显示是否存在新版本、是否引用缺失、是否本地文件丢失。
- 从引用对象可跳转到对应画布、分镜或素材详情。

验收标准：

- 用户能快速定位某个视频素材被哪些分镜使用。
- 删除或替换素材前能看到影响范围。
- 缺失文件或过期版本能被项目级发现。

不做事项：

- 不做自动修复。
- 不做跨项目引用分析。

## B 线：短剧生产闭环

### M6.6：项目工作流首页增强

目标：让项目详情页成为真正的制作看板，而不是入口集合。

状态：已实现，待页面验收。当前项目详情总览已增强为制作看板，展示画布、剧本、分镜、生成队列、已生成视频、失败生成、缺素材、过期引用、项目库素材和最近 Agent 任务摘要，并基于项目状态给出下一步建议；下一步继续 M6.6.1 一集一画布与剧本导入。

交付内容：

- 项目详情首页展示剧本、分镜、素材、生成队列、待验收视频、剪辑包状态。
- 显示关键指标：集数、场次数、分镜数、已生成视频数、失败数、缺素材数、过期引用数。
- 提供下一步建议：补剧本、补分镜、补素材、生成视频、质检、导出剪辑包。
- 每个指标可跳转到对应工作区，并带上项目筛选上下文。
- 保留现有 Agent 工作台入口，轻量展示最近任务和待确认动作。

验收标准：

- 用户打开项目后能知道当前制作进度。
- 用户能从项目首页进入当前最需要处理的工作。
- 页面不重复承载画布、分镜、素材的完整编辑能力，只做总览和跳转。

不做事项：

- 不做复杂甘特图。
- 不做多人实时看板。
- 不做自动排期。

### M6.6.1：一集一画布与剧本导入

目标：明确“一个画布对应一集内容”的生产边界。新建画布时可以导入或选择本集剧本，让剧本、设定库、分镜表、生成镜头组、生成队列和素材归档都围绕同一集展开。

状态：已实现，待页面验收。当前新建项目画布已支持不绑定剧本、选择已有分集、粘贴 / 导入本集剧本；画布保存 `episodeId / episodeTitle / scriptId / scriptSnapshot`，项目画布列表显示绑定集数，画布运行页顶部可打开本集剧本抽屉，生成素材 metadata 已带上本集上下文；下一步继续 M6.6.2 剧本资产拆解与资产图 Brief。

交付内容：

- 新建画布时支持导入本集剧本或选择项目中已有剧本分集。
- 画布保存 `episodeId`、`episodeTitle`、`scriptId`、`scriptSnapshot` 或等价引用信息。
- 项目详情中能看到每个画布对应哪一集。
- 画布内的剧本、分镜、生成队列和生成素材 metadata 优先记录本集上下文。
- 旧画布仍可不绑定集数，项目工作台中标记为未绑定集数。

验收标准：

- 新建画布时导入一集剧本后，画布详情能看到本集标题和剧本文本。
- 从该画布创建分镜、生成队列和素材时，metadata 能追溯到本集。
- 一个项目可以有多个画布，每个画布对应不同集数。

不做事项：

- 不做跨集时间线。
- 不强制旧画布绑定剧本。
- 不把整部剧所有集都塞进一个画布。

### M6.6.2：剧本资产拆解与资产图 Brief

目标：资产图先于分镜头组。导入本集剧本后，先提取这一集需要稳定下来的角色、场景、道具、风格和光影参考，再进入生图 Brief 生成资产图，保证后续分镜头组引用的是同一套资产。

状态：已实现，待页面验收。当前已新增本地资产拆解 store、规则草案生成、去重合并、设定库关联、Brief 草稿、手动素材绑定和素材 metadata 追溯；本轮不接真实 LLM，不自动生成资产图，已继续完成 M6.6.3 分镜头表与生成镜头组。

交付内容：

- 从本集剧本中整理资产清单：角色、场景、道具、风格 / 光影参考。
- 每个资产条目可关联到设定库中的角色、场景、道具。
- 每个资产条目可创建对应的生图 Brief：角色图、场景图、道具图或氛围参考图。
- 资产图生成成功后进入素材库，并回写到设定库 assetRefs。
- 资产拆解结果在项目 / 画布的生产流程中可见。

流程关系：

```text
本集剧本
  -> 资产拆解
      -> 角色资产 Brief -> 角色图 -> 设定库 / 素材库
      -> 场景资产 Brief -> 场景图 -> 设定库 / 素材库
      -> 道具资产 Brief -> 道具图 -> 设定库 / 素材库
      -> 风格光影 Brief -> 氛围参考图 -> 素材库
```

验收标准：

- 导入本集剧本后，用户能先得到本集资产清单。
- 用户能从资产清单进入 Brief 表单生成资产图。
- 生成的资产图能写回设定库和素材库。
- 后续分镜头组可以选择这些资产图作为参考。

不做事项：

- 第一版不做全自动资产确认。
- 不要求资产图依赖分镜头组后才能生成。
- 不把资产图只绑定到单个 15 秒生成片段。

### M6.6.R1：代码结构收口

目标：收拢 M6.6 / M6.6.1 / M6.6.2 / M6.6.3 累积代码里的重复拼装逻辑和过重 UI 区块，降低项目详情页、资产拆解抽屉、分镜管理抽屉和画布上下文相关代码继续膨胀的风险。

状态：已实现，待回归确认。当前已把新建画布的本集剧本绑定数据构造、资产拆解回写素材 metadata、资产拆解同步设定库 `assetRefs`、生成视频素材的分镜 / 生成镜头组 metadata 扩展下沉到可测试纯函数；M6.6.3 的分镜头表和生成镜头组 UI 已拆到同目录小组件。未移动缺少测试覆盖或容易影响 UI 行为的复杂组件逻辑。

交付内容：

- `canvas-episode-context` 增加新建画布剧本绑定的构造 helper。
- `asset-breakdown` 增加素材 metadata 和设定库引用的构造 helper。
- 项目详情页保留创建画布、绑定项目和跳转副作用，不再内联 episode metadata 拼装。
- 资产拆解抽屉保留 store 更新副作用，不再内联素材 metadata / 设定库引用拼装。
- 分镜管理抽屉保留 store 编排和副作用，分镜头表 / 生成镜头组卡片与表单拆到同目录组件。
- `canvas-generated-asset` 增加生成视频分镜 metadata 扩展 helper，避免内联重复拼字段。
- 补充纯函数测试，覆盖导入剧本绑定、metadata 回写、设定库引用角色映射。

不做事项：

- 不新增产品功能。
- 不改 UI 行为。
- 不改后端和 localforage key。
- 不移动缺少测试覆盖的大段页面组件。

### M6.6.3：分镜头表与生成镜头组

目标：把本集剧本拆成可编辑的分镜头表，再由用户把连续分镜头组合成可生成的视频单元。生成镜头组是视频生成和生成表管理的基本单位，必须不超过 15 秒、同一个场次、不跨场景。

状态：已实现，待页面验收。当前分镜管理抽屉已新增“分镜头表 / 生成表”，可从已绑定本集剧本的画布生成分镜头草案、人工编辑 / 删除 / 排序、选择连续镜头组合生成镜头组、编辑视频提示词和绑定素材 / 设定；在具体画布中可确认“打组加入画布”，只创建提示词节点、参考素材节点和视频生成配置节点，不自动触发真实生成。

核心概念：

- 分镜头：剧本拆分后的单个镜头，包含画面描述、人物动作、对白、情绪、景别、时长、场次和场景。
- 生成镜头组：由一个或多个连续分镜头组合而成，用于生成一个视频段落。
- 生成表：按生成镜头组管理提示词、资产引用、音频引用、状态、任务和结果。

交付内容：

- 新增或增强分镜头表视图，支持按本集剧本生成分镜头草案。
- 受控 Agent 可把本集剧本拆成分镜头表草案；用户必须能逐条编辑、删除、排序和补充。
- 用户可选择多个连续分镜头组合成生成镜头组。
- 生成镜头组校验规则：总时长不超过 15 秒、同一个场次、不跨场景。
- 生成镜头组支持生成或编辑视频提示词。
- 生成镜头组支持绑定图片资产、音频资产、参考视频和设定库条目。
- 生成表展示每个生成镜头组的提示词、资产、状态、taskId、结果素材和主版本。
- 生成镜头组可打组加入画布，画布展示节点关系，生成表负责管理结构化状态。

流程关系：

```text
本集剧本
  -> 分镜头表草案
  -> 人工修分镜头
  -> 选择连续分镜头
  -> 生成镜头组
  -> 生成视频提示词
  -> 绑定资产图 / 音频 / 参考视频
  -> 加入画布生成
  -> 结果回流生成表 / 素材库
```

验收标准：

- 用户能从一集剧本生成分镜头表草案。
- 用户能独立编辑每个分镜头内容。
- 不能把跨场景或总时长超过 15 秒的分镜头组合成生成镜头组。
- 生成表能看到每个生成镜头组的提示词、资产、状态和结果。
- 画布仍展示生成节点和关系，但不承担完整表格管理职责。

不做事项：

- 不做无确认自动生成视频。
- 不把跨场景内容强行合并为一个生成镜头组。
- 不要求生成镜头组替代分镜头表。

### M6.7：生图 Brief 工作台

目标：把生图从“直接写提示词”升级为“先完成导演和美术思考，再生成图片”。Brief 工作台承接剧本资产拆解、设定库、分镜、参考图和提示词仓库，帮助用户在生成场景图、角色图、道具图、分镜氛围图前补齐关键信息、检查质量、拼装提示词，并把 Brief 快照和结果一起归档。

模型推理程度：

- 第一版制作使用 `高`：需要跨结构化 Brief 数据、store、表单、校验、提示词拼装、生成链路和素材 metadata 归档。
- 不使用 `超高` 做第一版：自动深度读剧本、自动拆场景、自动推荐电影 / 广告参考、自动生成完整美术方案先放到后续增强。

产品模式：

- 标准模式：场景、角色、道具生成前必须填完整核心项，否则不能生成。
- 提醒模式：系统提示缺了什么，但允许继续生成；默认使用该模式。
- 自由模式：屏蔽标准检查，只保留普通提示词生成和归档。

入口规划：

- 项目详情可进入“生图 Brief 工作台”。
- 画布图片生成配置节点可打开 Brief 抽屉。
- 本集资产拆解条目可直接创建角色图、场景图、道具图或氛围图 Brief。
- 设定库中的角色、场景、道具可从对应条目创建 Brief。
- 分镜条目可基于当前分镜创建氛围图或参考图 Brief。

第一版交付内容：

- 新增场景图、角色图、道具图三套结构化 Brief 表单。
- Brief 支持关联剧本片段、分镜条目、设定库条目和参考素材。
- 支持标准模式 / 提醒模式 / 自由模式。
- 新增 brief validator，按类型检查缺项、参考图用途和质量风险。
- 新增 prompt builder，把结构化内容拼成最终生图提示词。
- 最终提示词允许人工修改，并保存原始结构化 Brief 与最终提示词。
- 接入现有图片生成节点或图片生成入口。
- 生成成功后，图片自动入库，并在素材 metadata 中保存 Brief 快照、最终提示词、参考图和来源。

场景图核心字段：

- 剧本片段、本场情绪、场景拆分、地点、时间、天气氛围。
- 功能区域、道具陈设、人物与站位。
- 主光、辅光、逆光、光质、光比。
- 构图方式：纵深、三分法、对称、前景中景后景。
- 参考图用途：构图参考、灯光氛围、场景风格、禁用参考。
- 风格参考：电影感、写实、农村、科技、梦幻、黑暗、压抑等。

角色图核心字段：

- 剧本角色片段、角色身份、年龄、气质、当场情绪。
- 服装、发型、妆造、表情、姿态、视线方向。
- 拍摄景别：半身、全身、特写。
- 面部光质、背景或所在场景、角色一致性参考图、禁止跑偏项。

道具图核心字段：

- 道具名称、剧情作用、所属角色或场景。
- 年代、材质、尺寸、磨损程度。
- 摆放位置、是否需要被人物手持或使用。
- 光影质感、构图方式、细节参考图。

第二阶段方向：

- 模型推理程度升级为 `超高`，用于更复杂的剧本理解和美术指导判断。
- 粘贴剧本后自动分析情绪、人物、场景、道具。
- 自动建议是否拆成 2-3 个场景。
- 自动推荐需要哪些参考图类型。
- 自动生成初版 Brief，用户只需要修改和确认。

第三阶段方向：

- 每场戏形成场景资产包。
- 角色、道具、场景互相关联。
- 出图版本对比。
- 可导出为美术设定表、生图提示词表、分镜资产表。

验收标准：

- 用户能从场景 Brief 填写核心信息并生成最终提示词。
- 提醒模式下缺少关键字段会提示，但允许继续生成。
- 标准模式下缺少关键字段不能生成。
- 参考图必须能标注用途，并写入 Brief 快照。
- 生成成功图片能自动入库，素材详情能追溯 Brief、提示词和参考图。

不做事项：

- 第一版不接复杂 Agent 编排。
- 第一版不做全自动剧本理解。
- 第一版不做独立于项目体系之外的孤立工具。
- 第一版不替代现有设定库，只扩展设定库可承接的结构化 Brief 数据。

### M8：生成历史与任务日志打通

目标：让前台素材、画布节点、分镜结果和后台 `ai_tasks` 账本互相可追溯。

交付内容：

- 生成成功入库时记录后端 `aiTaskId`。
- 素材详情展示任务状态、扣费、失败原因、刷新状态入口。
- 画布视频节点任务详情可跳转或定位对应后台任务。
- 后台 AI 任务日志可反查素材、画布节点和分镜条目。
- 失败返还、素材状态、分镜状态保持一致。

验收标准：

- 用户能从素材详情看到生成任务和扣费信息。
- 管理员能从任务日志定位到对应素材和画布节点。
- 失败任务的前台状态、后台状态和返还状态能对齐。

不做事项：

- 不把本地直连任务强行写入云端账本。
- 不下发私密渠道配置。

## C 线：云端资产与协作

### M10.1：云端文件表与对象存储底座

目标：为项目共享资产库云端版建立文件存储、权限和引用基础。

交付内容：

- 后端新增文件表，记录 fileKey、mimeType、bytes、checksum、provider、createdBy。
- 增加对象存储配置，第一版支持 S3 兼容协议。
- 提供上传、下载签名 URL、删除标记接口。
- 前端保存云端文件引用时使用 `fileId`，本地缓存继续使用 `storageKey`。
- 文档更新本地文件、云端文件和缓存的关系。

验收标准：

- 上传图片、视频、音频后，数据库只保存元数据和对象 key。
- 非授权用户不能获取签名 URL。
- 前端能用签名 URL 预览云端素材。

不做事项：

- 不做多端自动同步。
- 不在没有引用计数前物理删除对象。

### M10.2：项目共享资产库云端版

目标：让项目成员共享同一套项目资产，而不是只靠本地 metadata 标记。

交付内容：

- 后端新增项目资产、项目资产版本、项目文件夹、项目成员权限。
- 项目资产支持图片、视频、音频上传、列表、详情、标签、文件夹和版本。
- 前端素材页支持来源切换：本地素材、项目资产库、全部。
- 从项目资产库插入画布时记录 projectAssetId、versionId、fileId。
- 生成结果可选择归档到本地素材或项目资产库。

验收标准：

- 同一项目成员能看到同一套项目资产。
- 非项目成员不能访问项目资产。
- 画布引用项目资产后能稳定预览并记录版本。

不做事项：

- 第一版不做实时多人编辑。
- 不自动迁移所有本地素材。

### M10.3：云同步与冲突策略

目标：设计并逐步实现画布、分镜、素材 metadata 的多端同步。

交付内容：

- 定义本地 `storageKey` 与云端 `fileId` 双轨结构。
- 定义画布 JSON、分镜 JSON、设定库 JSON 的云端保存策略。
- 定义冲突检测：本地更新时间、远端更新时间、版本号。
- 冲突时提示用户选择保留本地、使用云端或另存副本。
- 增加同步状态展示：本地、同步中、已同步、冲突、失败。

验收标准：

- 同一个项目在不同设备上能看到云端项目资产。
- 冲突不会静默覆盖用户本地内容。
- 大体积媒体不写入画布 JSON。

不做事项：

- 第一版不做 Google Docs 式实时协同。
- 不在没有冲突策略前自动覆盖远端或本地数据。

## 已完成能力索引

这些能力已实现第一版，具体待测记录见 `docs/pending-test.md`。

- M5.0 数据底座：素材去重写入、生成信息、来源链路。
- M5.1 生成素材自动归档：画布图片和视频自动入库。
- M5.2 素材详情增强：生成提示词、参数、引用资产和来源节点展示。
- M5.3 画布项目预设：分辨率、画幅、帧率、默认模型等。
- M5.3.5 项目工作台总入口：项目下统一进入画布、剧本、分镜、设定库、素材、提示词和 Agent。
- M5.4 角色场景设定库：角色、场景、道具和一致性参考。
- M5.5 提示词仓库完善：结构化模板、变量、正向词、负向词和工作流模板。
- M5.6 剧本与集数管理：故事大纲、分集、场次和设定库引用。
- M5.7 分镜管理：分镜组、分镜条目、参考素材、打组加入画布。
- M5.8.1 分镜生成结果回流：视频生成结果回写分镜条目。
- M5.8.2 批量生成队列与预算控制：分镜批量生成、并发、重试和成本预估。
- M5.9 AI 短剧 Agent 工作台：资产管理员、提示词工程、分镜导演三个本地规则 Agent。
- M5.10 Agent Skill 系统：本地 AgentSkill 协议、registry 和四个内置 Skill。
- M6.1 生成前提示词自审：本地规则自审和生成前确认弹窗。
- M6.2 渠道边界收口：本地直连只保留 OpenAI 兼容，火山方舟 / Seedance 走云端渠道。
- M6.5 成片交接与剪辑包：按分镜导出视频、参考素材、提示词和清单。
- M6.6.1 一集一画布与剧本导入：画布绑定本集上下文和剧本快照。
- M6.6.2 剧本资产拆解与资产图 Brief：本集资产清单、设定库关联和 Brief 草稿桥接。
- M6.6.3 分镜头表与生成镜头组：本地规则拆镜头、组合 15 秒生成镜头组和打组加入画布。
- M6.6.R1 代码结构收口：拆分 M6.6 累积 UI 和纯函数边界。
- M6.7 生图 Brief 工作台：场景图、角色图、道具图、氛围参考图 Brief 数据、校验、prompt 拼装和轻量工作台。
- M6.7.1 Brief 接入生图与结果归档：Brief 创建图片配置节点、生成素材归档 metadata 和来源回写。
- M6.7.R1 Brief 工作台结构收口：拆分 Brief 卡片 / 表单 UI，保持行为和 localforage key 不变。
- M6.7.2 Brief 结果版本对比与主参考图强化：展示多次出图结果、素材当前版本号、主参考图切换和显式同步来源。
- M6.7.3 Brief 导出为美术设定表 / 生图提示词表：按三种视图导出 CSV / JSON，不修改业务数据。
- M7.0 素材资产中心筛选排序：按分镜组、生成时间、更新时间、标题等筛选排序。
- M7 批量整理与导入校验：批量移动、标签、删除、素材包校验、火山加白批处理。
- M7.1 项目共享资产库本地底座：本地 projectLibraries metadata、项目库筛选和标记。
- M7.2 素材版本历史：替换内容生成版本、恢复旧版本、版本历史展示。
- M7.2.2 素材版本引用锁定：画布、分镜、设定库引用素材时记录版本快照，并可手动更新到最新版。

## 需求插入规则

后续新增需求按以下方式放入路线图：

- 如果影响项目总入口、进度、生产闭环，优先放入 B 线。
- 如果涉及一集一画布、剧本导入、剧本资产拆解、分镜头表、生成镜头组、生成表，优先放入 M6.6.x。
- 如果是生成前标准化、Brief 表单、质量检查、提示词拼装，优先放入 M6.7 生图 Brief 工作台。
- 如果影响素材、版本、引用、导入导出、资产安全，优先放入 A 线或 C 线。
- 如果涉及云端文件、多人共享、权限、对象存储，优先放入 C 线。
- 如果是本机执行器、CLI、桌面端本地能力，暂不进入近期规划；先记录为远期想法，等主工作台生产闭环稳定后再评估。
- 如果是 Agent 分析、建议、质检、调度，暂不进入近期规划；先记录为远期想法，等生产闭环和云端资产稳定后再评估。
- 剧本拆分镜头表属于 M6.6.3 的受控草案生成能力，不纳入暂缓的 Agent 进阶编排。
- 所有会写入数据、扣费、生成视频、覆盖版本或删除内容的 Agent 能力，必须保留用户确认步骤。

## 指令推理程度规则

后续给开发指令时必须附带模型推理程度，帮助控制额度消耗：

- `中`：文档整理、简单 UI 调整、单文件工具函数、小范围筛选或展示逻辑。
- `高`：跨多个前端模块的数据结构、store、表单、校验、metadata 写入、生成链路接入。
- `超高`：后端表结构、云端对象存储、任务账本、权限、同步冲突、复杂跨端链路。

默认优先使用 `中`。只有涉及跨模块架构判断、数据一致性、云端安全或长链路生成时才提升到 `高` 或 `超高`。

## 暂缓池

- Agent 进阶编排：质检 Agent、生成调度 Agent、剪辑顾问 Agent、合规 Agent 暂不进入近期规划。
- 即梦 CLI 本机版：本机执行器、CLI 队列、CLI 与主工作台桥接暂不进入近期规划。
- M7.5 视频后处理工具箱：火山视频超分、字幕抹除、补帧、增强等后处理能力暂不进入近期规划，避免过早牵出后端任务、扣费、上游接口和素材派生复杂度。
