# 当前工具全面验收清单

本文档用于验收当前 `v0.2.82` 版本的 AI 画布工具。验收目标不是证明每个历史功能都存在，而是确认用户能沿当前主路径完成一次短剧生产闭环，并能定位阻断风险。

## 验收结论口径

- 通过：P0 / P1 项全部通过，P2 项无明显主流程阻断，文档和版本记录能解释当前状态。
- 有条件通过：P0 项通过，存在 P1 风险但有明确绕行路径，且不影响继续人工试用。
- 不通过：任一 P0 项失败，或 P1 项导致项目、分集、画布、素材、AI 配置、生图 / 视频承接任一主路径无法继续。

## 验收前置

1. 确认当前分支干净：`git status --short` 无输出。
2. 确认版本一致：`VERSION`、`web/package.json`、`web/desktop/app/package.json` 均为 `0.2.82` / `v0.2.82`。
3. 启动前端：在 `web/` 下运行 `npm run dev`，默认访问 `http://localhost:3000`。
4. 如需验证后端代理和管理后台，启动 Go 后端并确认 `/api/*` 代理可用。
5. 准备素材：至少 3 张图片、1 个视频、1 个音频、1 段短剧剧本文本、1 份 AI 配置包 JSON。

## 五项目标验收记录

本轮按“能不能用、好不好用”的合格线执行五项目标验收。结论：有条件通过，不是最终发布通过。

| 目标 | 当前结论 | 证据 | 剩余风险 |
| --- | --- | --- | --- |
| 真实单集能完整跑完 | 部分通过 | 使用隔离浏览器新建“五项目标验收短剧”，导入《第一集：雨夜重逢》，进入本集生产流程，创建承接画布并进入 `/canvas/:id`；画布刷新后仍显示本集标题、返回本集生产流程、工具栏和版本 `v0.2.82` | 当前环境后端 8080 未启动，真实 Agent 文本调用 `/api/v1/chat/completions` 返回 502，无法证明导演分析到分镜视频节点的真实 AI 完整链路已跑完 |
| 生成失败后能继续 | 通过 | 未启动后端时点击“运行分析”，阶段变为“导演分析异常”，错误提示为“接口连接失败，请确认后端服务已启动”，`运行分析` 仍可点击，剧本、项目和分集未丢失 | 仍需在真实 API Key 和后端环境中验证模型超时、返回空内容、结构化 JSON 异常后的继续能力 |
| 中途不丢数据 | 通过本地路径 | 创建项目、分集、承接画布后刷新画布，再返回本集流程和项目详情；项目详情仍显示第 01 集、草稿状态、画布数 1 | 当前数据保存在浏览器本地；隔离浏览器或换设备无法看到同一项目，不能误判为已支持云同步 |
| 新用户 30 分钟理解主流程 | 有条件通过 | 项目空态提示“先新建或导入第一集剧本”；本集流程显示“导演分析 / 资产与生图 / 分镜生产包 / 画布承接”和阻塞状态；资产空态显示前置阶段未批准；承接画布弹窗说明不会自动生成图片或视频 | 仍需真实新用户走查；目前 `docs/pending-test.md` 过长，不能作为新用户入口，只能作为开发验收索引 |
| 云部署前专项检查 | 不通过，需补环境 | 前端本地兜底可运行，但 `/api/settings`、`/api/auth/*`、`/api/v1/chat/completions` 在后端 8080 未启动时均返回 502；`docs/cloud-assets-plan.md` 明确当前云资产仍是方案，不是已实现能力 | 部署前必须启动并验证 Go 后端、认证、公共设置、AI 代理、对象存储或本地文件策略；不能承诺项目、素材、画布已云同步 |

本轮额外确认：

- 未运行真实扣费生图或视频生成。
- 未执行构建；页面请求和浏览器交互用于验收证据。
- 未配置后端和真实 API Key 的环境只能证明“失败后可继续”和“本地数据不丢”，不能证明 AI 生产链路最终可用。

## P0 主流程验收

### 1. 核心入口与导航

- 入口：`/`、`/projects`、`/image`、`/prompts`、`/assets`、`/canvas`、`/canvas/:id`。
- 操作：
  1. 打开 `/`，未登录时确认进入 `/login?redirect=/projects`；登录后应回到项目工作台。
  2. 打开 `/canvas`，确认回到项目工作台，不进入旧画布库。
  3. 打开 `/image`、`/prompts`、`/assets`，确认统一使用左侧工作区导航。
  4. 在左侧栏点击项目工作台、生图工作台、提示词库、我的素材和 AI 配置。
- 通过标准：
  - 登录后的项目工作台是主入口。
  - 独立画布库旧入口不再干扰主流程。
  - 工具页入口、退出登录和 AI 配置入口可达。
- 失败影响：用户无法理解从哪里开始，或会被旧入口带离当前工作流。

### 2. 项目与分集生产入口

- 入口：`/projects`、`/projects/:id`。
- 操作：
  1. 新建项目，填写项目名称并选择预设。
  2. 打开项目详情页，点击“新建分集”并粘贴剧本文本。
  3. 导入后确认进入 `/projects/:id/episodes/:episodeId/workbench`。
  4. 从项目详情页返回项目工作台，再重新进入该项目和该分集。
- 通过标准：
  - 项目可创建、打开、重命名、归档或删除。
  - 分集导入后能直接进入本集生产流程。
  - 项目详情能表达当前制作进度和下一步入口。
- 失败影响：短剧生产闭环的起点失效。

### 3. 本集生产流程

- 入口：`/projects/:id/episodes/:episodeId/workbench`。
- 操作：
  1. 检查左侧阶段导航和当前阶段状态。
  2. 在导演分析、资产与生图、分镜生产包、画布承接各模块中分别打开详情或预览。
  3. 执行不会真实扣费的预览 / 确认 / 采用类操作。
  4. 在画布承接中创建或打开承接画布。
- 通过标准：
  - 阶段状态、阻塞原因和下一步操作清楚。
  - 结构化输出摘要可读，不直接暴露大量 JSON 字段。
  - 画布承接不会跳到旧抽屉流程。
- 失败影响：项目虽然可进入，但用户不知道下一步怎么推进。

### 4. 画布核心操作

- 入口：`/canvas/:id`。
- 操作：
  1. 从项目或本集流程打开已绑定项目 / 分集的画布。
  2. 验证画布拖拽、缩放、选择、框选、撤销、重做、删除、清空确认。
  3. 使用底部工具栏新增文本、图片、视频、音频和生成配置节点。
  4. 一次拖入多张图片 / 视频 / 音频，确认批量生成节点且不完全重叠。
  5. 点击“素材”和“本集生产流程”入口。
  6. 点击画布左上角返回入口。
- 通过标准：
  - 画布基础编辑稳定。
  - 已绑定分集时直接返回正式本集生产流程。
  - 未绑定上下文时有兜底绑定入口。
- 失败影响：核心创作载体不可用。

### 5. 素材管理与引用

- 入口：`/assets`、项目详情素材引用入口、画布素材入口。
- 操作：
  1. 新建文本、图片、视频、音频素材。
  2. 建立文件夹，移动素材，批量添加标签，导出 / 导入素材包。
  3. 将素材加入项目库，并从画布或分镜引用该素材。
  4. 替换素材形成新版本，检查旧版本下载、过期引用筛选和批量更新。
- 通过标准：
  - 素材本体、版本、项目库归属和引用关系可见。
  - 更新到最新版只修改引用方，不静默污染素材本体。
  - 本地文件缺失或旧版本引用能被发现。
- 失败影响：用户不敢替换、删除或复用素材。

### 6. AI 配置与模型边界

- 入口：左侧栏或顶部“AI 配置”弹窗、`/api/ai-config-template`。
- 操作：
  1. 下载配置模板，确认包含渠道、Base URL、API Key、模型、视频协议和思考模式字段。
  2. 修改模板后导入配置包，关闭并重新打开配置弹窗。
  3. 切换本地直连模式，开启 / 关闭思考模式。
  4. 使用缺失配置状态触发生图或画布生成，确认会提示先配置。
- 通过标准：
  - 配置包可导入、保存和恢复。
  - 生图接口不附加文本思考参数。
  - 本地直连和云端渠道边界符合 `docs/todo.md` 描述。
- 失败影响：AI 能力不可用或渠道边界混乱。

### 7. 生图工作台

- 入口：`/image`，以及本集生产流程“进入生图工作台”。
- 操作：
  1. 从本集资产提示词进入生图页，确认自动带入项目 / 分集 / 资产上下文和提示词。
  2. 添加本地参考图和剪贴板参考图。
  3. 检查左侧生成历史摘要加载速度，点击历史记录后再加载图片详情。
  4. 在配置完整的环境中生成图片，并保存到素材。
- 通过标准：
  - 生图页布局三栏清晰，右侧参数不被挤压。
  - 来源上下文能回写到素材 metadata。
  - 历史记录懒加载不影响回填。
- 失败影响：资产到生图、生成到素材的闭环断裂。

### 8. 视频 / 生产包承接

- 入口：本集生产流程“分镜生产包 / 画布承接”、`/video` 抽样。
- 操作：
  1. 在本集生产流程检查生产包状态、提示词、参考资产、风险提示和导入按钮。
  2. 导入生产包到承接画布，确认创建视频配置节点并聚焦。
  3. 打开 `/video`，确认它不是主生产入口，只作为低优先级审核 / 演示页存在。
  4. 在画布视频节点中检查时长、比例、音频开关和生成配置同步。
- 通过标准：
  - 正式视频链路从项目 / 分集上下文进入。
  - 独立 `/video` 不干扰主路径。
  - 视频时长不被强制锁定为 10 秒，音频默认关闭。
- 失败影响：用户可能把视频生产从项目上下文里做散，或生成参数错误。

## P1 回归验收

1. 登录 / 退出：普通用户和管理员都能从工作区侧栏退出，退出后进入登录页。
2. 管理后台：后台页面可进入，底部入口指向项目工作台。
3. 提示词库：提示词搜索、详情、复制、加入素材、插入工作流仍可用。
4. 画布助手：会话切换、发送消息、插入文本 / 图片、折叠恢复可用。
5. 设定库：角色、场景、道具可新增、编辑、绑定素材和删除。
6. 分镜管理：分镜组、分镜条目、生产包、加入画布和结果版本管理可用。
7. 键盘可访问性：项目卡片、视图切换、画布工具栏、设置面板控件有清晰焦点态。
8. 暗色 / 浅色主题：画布按钮、弹窗和浮层不出现硬编码黑白导致的断层。

## P2 文档与版本验收

1. `docs/pending-test.md` 顶部必须能说明当前版本最优先验收项。
2. `docs/todo.md` 必须仍把项目工作台、本集生产流程和本地优先边界作为当前主线。
3. `docs/features.md` 不应把仍待验收的本地能力写成已经稳定发布能力。
4. `VERSION`、`web/package.json`、`web/desktop/app/package.json` 版本必须一致。
5. `CHANGELOG.md` 的 `Unreleased` 若继续为空，应在发版前补齐当前版本归纳。

## 当前静态取证

- 已确认路由入口存在：项目、分集、画布、生图、视频、提示词、素材、登录、管理后台和 AI 配置模板 API。
- 已确认版本文件一致：`VERSION` 为 `v0.2.82`，Web 和桌面包版本均为 `0.2.82`。
- 已确认当前提交干净基线：`63f6571 提交当前工作区内容` 后工作区无未提交变更。
- 已发现文档风险：`CHANGELOG.md` 仍停留在 `v0.2.3` 以前的正式记录，当前 `v0.2.82` 大量待测能力只记录在 `docs/pending-test.md`，发版前需要整理版本级摘要。

## 本轮自动化验收记录

- HTTP 路由验收：
  - `/` 返回 307 到 `/login?redirect=/projects`，符合未登录保护路径。
  - `/projects` 返回 200，页面内容包含“项目工作台”“生图工作台”“提示词库”“我的素材”“AI 配置”。
  - `/canvas` 最终落到 `/projects`，不再进入旧画布库。
  - `/image`、`/prompts`、`/assets` 均返回 200，并包含统一工作区导航文案。
  - `/video` 返回 200，页面内容包含“视频提示词审核台”，符合低频审核页定位。
  - `/api/ai-config-template` 返回 200，JSON 包含 `aiConfig`、`thinkingMode`、`reasoningEffort`。
- Headless Chrome 交互验收：
  - 使用独立临时浏览器 profile 打开 `/projects`，页面显示工作区左侧栏、项目工作台、AI 配置入口和“新建项目”。
  - 点击“新建项目”，弹出项目创建弹窗，包含项目名称、常用预设、分辨率、画幅比例、帧率、默认时长、默认模型和“创建项目”。
  - 填写“验收项目”并创建后，自动进入 `/projects/:id`，项目详情显示分集、设定库、素材、画布、当前制作状态和“新建分集”。
  - 点击“新建分集”，弹出“导入本集剧本”，填写“第一集”和剧本文本后点击“导入并进入生产流程”。
  - 页面进入 `/projects/:id/episodes/:episodeId/workbench`，本集生产流程显示剧本已导入、导演分析未开始、资产与生图已阻塞、分镜生产包已阻塞、画布承接未绑定，并提供“返回项目”“创建承接画布”“运行分析”等入口。
  - 在本集生产流程点击“创建承接画布”，弹出创建承接画布弹窗，说明该画布会绑定当前集且不会自动生成图片或视频。
  - 点击“创建并进入”后进入 `/canvas/:id`，画布显示“返回本集生产流程”、当前集标题、生产包 P01 / P02 / P03、底部工具栏“文本 / 图片 / 视频 / 音频 / 生成配置 / 上传素材 / 素材 / 本集生产流程 / 画布外观 / 清空画布”，以及右侧生产包绑定面板。
  - 画布页未执行节点拖拽、批量文件拖入或真实生成，只验证了承接画布入口、上下文返回和工具栏可达。
- 前端逻辑测试：
  - 执行 `npm run test`。
  - 首次运行发现 1 个测试预期未同步：分镜组插入画布 metadata 已新增 `productionPackageId`、`productionPackageTitle`、`productionPackageRole`，测试仍按旧合同断言。
  - 已同步测试预期后重跑，结果为 430 通过、0 失败。
- 未覆盖项：
  - 本轮已覆盖项目创建、分集导入和进入本集生产流程的真实浏览器交互。
  - 尚未覆盖画布节点拖拽 / 缩放 / 批量文件拖入、素材编辑和版本弹窗、AI 配置弹窗导入文件、真实 AI 生图 / 视频请求、视频生产包导入到画布后的节点视觉检查。
  - 因此本轮结论仍不能替代完整人工页面验收或后续 E2E 验收。

## 初步风险列表

| 优先级 | 风险 | 影响 | 建议 |
| --- | --- | --- | --- |
| P0 | 本集生产流程和画布承接链路很长，容易出现某个入口仍打开旧抽屉或旧页面 | 用户迷路，视频生产上下文丢失 | 按 P0-3、P0-4、P0-8 连续跑一遍 |
| P1 | `docs/pending-test.md` 内容过长，人工验收容易漏项 | 通过结论不可信 | 使用本文档作为执行入口，pending-test 只作为明细索引 |
| P1 | `/video` 已从主导航隐藏但路由仍存在 | 用户可能误把它当正式主流程 | 验收时明确只抽样，不作为主生产入口 |
| P1 | 大量数据存在浏览器本地 | 换浏览器 / 清缓存会丢数据 | 验收前后都记录本地优先边界，避免误判为云同步 |
| P1 | 当前 3000 dev server 可能未随版本文件重启，画布页实测出现 `v0.2.76`，但源码会从 `VERSION` 注入 `v0.2.82` | 运行态版本展示可能误导验收和发包判断 | 最终验收前重启 dev server，并重新确认版本按钮显示 `v0.2.82` |
| P2 | `CHANGELOG.md` 与当前版本差距大 | 发版说明失真 | 发版前把当前 `pending-test` 归纳进新版记录 |

## 本轮验收结论

当前完成了验收清单、HTTP 路由验收、配置模板验收、项目 / 分集 / 本集生产流程 / 承接画布入口浏览器交互验收和前端逻辑测试验收。结论为：有条件通过。

有条件通过的边界是：核心入口、项目创建、分集导入、进入本集生产流程和创建承接画布没有发现 P0 阻断，测试已全绿；但画布编辑、素材版本、配置包文件导入和真实 AI 调用尚未执行，不能视为最终发布验收通过。下一步应先重启当前 dev server 确认版本显示为 `v0.2.82`，再按 P0 主流程从“画布节点操作 -> 素材 / 生图 / 视频配置回归”继续人工或 E2E 执行，并记录每项通过、失败或无法验证的证据。
