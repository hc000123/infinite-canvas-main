# 待测试

- M7 批量删除与素材包导入校验第一版已实现，版本提升到 `v0.2.21`：我的素材页在已选素材工具条新增“删除选中”，需要确认弹窗后才会逐个走现有 `removeAsset` 删除并触发原有本地媒体清理逻辑；素材包导入新增结构校验，只接受本工具导出的 `app=infinite-canvas/version=1` 素材包，校验 `assets.json`、素材类型和文件清单，不合法时直接提示错误原因，避免坏包半导入。已新增单元测试覆盖有效素材包、非本工具包、不支持素材类型和错误文件清单；仍需真实页面复测：多选素材后删除选中、取消不删除、确认后列表和刷新后状态正确；导入有效素材包成功，导入缺 `assets.json` 或伪造包时显示明确错误。
- M7 批量整理第一版已实现，版本提升到 `v0.2.20`：我的素材页在已选素材工具条新增“移动文件夹”和“添加标签”。用户可先全选当前筛选结果或手动勾选素材，再批量移动到指定文件夹或未分组；批量添加标签会保留原有标签并做大小写不敏感去重，不改变素材文件和 localforage 存储结构。已新增单元测试覆盖标签归一化、批量移动 patch 和批量标签合并；仍需真实页面复测：选择多个素材后移动到新建文件夹、移动回未分组、批量追加标签后打开素材详情确认标签保留且不重复、刷新页面后批量整理结果仍保存。本轮不改后端、不做云端项目共享资产库。
- M7.0 素材资产中心分镜筛选与排序已实现，版本提升到 `v0.2.19`：我的素材页新增“分镜组筛选”，可在项目上下文内选择分镜组，筛选结果会包含该分镜组直接引用的参考素材、`resultAssetIds/primaryAssetId` 指向的生成结果素材，以及 generation metadata 中记录 `storyboardGroupId` 的素材；素材列表工具条新增排序，可按默认、最近更新、最近生成、创建时间和标题排序。已新增单元测试覆盖分镜组引用素材收集、generation metadata 归属筛选和排序；仍需真实页面复测：进入某个项目的“我的素材”，选择分镜组后能看到该组参考图/参考视频和生成主版本视频，切换“最近生成/最近更新/标题”排序后列表顺序符合预期。本轮不新增云端共享资产库、不改后端、不改变素材存储结构。
- M6.5 成片交接与剪辑包第一版已实现，版本提升到 `v0.2.18`：分镜管理抽屉新增“导出剪辑包”按钮，可按当前分镜组生成本地 zip，包含 `shots.json`、`shots.csv`、`prompts/` 分镜提示词文本、`videos/` 主版本视频文件和 `references/` 参考素材文件。导出清单会按分镜顺序命名，记录每条分镜的提示词、实际提交提示词、状态、主版本素材、视频路径、时长、模型、供应商、taskId、生成方式、参数和参考素材；导出前会检查缺少主版本、失败分镜、主版本不是视频、缺少本地视频文件和异常时长，用户可返回检查或继续导出清单。已新增单元测试覆盖导出清单顺序、媒体请求、提示词文件、CSV 转义和导出前警告；仍需真实页面复测：在分镜组中选择有主版本视频的分镜，点击“导出剪辑包”，确认压缩包内 `shots.json/shots.csv/prompts/videos/references` 文件完整，缺失主版本或失败分镜时会先出现导出前检查弹窗。
- M6.2 渠道边界收口第一版已实现，版本提升到 `v0.2.17`：本地直连边界收窄为 OpenAI 兼容能力，本地模式会强制 `videoProtocol=openai`，不再从浏览器本地配置向视频请求附带 `_volcengine_api_key/_volcengine_base_url` 或 `X-Volcengine-*` 头；`shouldAttachLocalVolcengineCredentials` 现在始终返回 false。前台配置弹窗本地直连区只展示 OpenAI 兼容 Base URL / API Key / 模型列表，不再展示火山方舟 Ark API Key、Ark Base URL、Seedance Endpoint，也不再提供本地 Seedance 视频模型切换；云端渠道说明明确火山方舟、Seedance Endpoint、额度和任务日志由后台维护。画布视频配置节点、视频节点提示词面板、`/video` 视频创作台都移除了本地“火山 Seedance”切换；旧节点或旧项目预设在本地模式下会被归一为 OpenAI 兼容 provider，云端模式仍可通过 Seedance 模型名或 `ep-` 模型推断 `volcengine-ark` 并构造 Ark JSON payload，但不携带前端本地 Key。已更新单元测试覆盖本地协议强制 OpenAI、远程 Seedance 模型推断、本地不附带 Ark 凭据、本地项目预设不绕回 Ark；仍需真实页面复测：配置弹窗本地模式不出现 Ark/Endpoint 输入，画布和 `/video` 本地模式不出现火山 Seedance 切换，旧本地 Ark 配置不会参与请求，云端 Seedance 模型仍能走后台私有渠道。
- M6.1 生成前提示词自审第一版已实现，版本提升到 `v0.2.16`：新增本地 `canvas-prompt-review` 纯函数，视频生成前可按主体/动作/场景完整性、否定表达、风格冲突、短时长动作数量、参考素材用途、Seedance 编辑/延长模式匹配度输出 `pass / warning / risk` 自审结果。视频设置新增“生成前提示词自审”开关，默认开启，可保存在全局视频配置和画布视频配置节点 metadata 中；自审不进入上游 payload，不创建云端 `ai_tasks`，不扣额度。画布视频生成链路在真正提交任务前执行本地自审，发现 warning/risk 时弹窗展示问题和建议，用户点“返回修改”不会创建任务，点“仍然生成”才继续原视频生成流程；自审关闭时保持原流程。已新增单元测试覆盖通过提示词、主体动作缺失、否定词过多、参考素材用途缺失、编辑/延长模式不匹配和开关默认行为；仍需真实页面复测：视频配置节点开关保存、明显问题提示词弹出自审、返回修改不创建任务、仍然生成正常创建任务、关闭自审后直接生成。
- M5.10 Agent Skill 系统第一版已实现，版本提升到 `v0.2.15`：短剧 Agent 工作台入口保持 `/projects/:id/agent` 不变，但任务生成改为通过本地 `AgentSkill` registry 选择技能；新增协议字段 `id/name/agentKind/description/version/inputSchema/outputSchema/riskLevel/run/apply`，不改后端、不接真实 LLM、不做插件市场或远程下载。当前内置四个本地规则 Skill：`asset.gap_check` 资产缺口检查、`asset.reuse_duplicate_scan` 素材复用/重复检测、`prompt.storyboard_completion` 分镜提示词补全、`storyboard.scene_to_draft` 剧本场次转分镜草案。新建 `AgentTask` 会记录 `skillId/skillName/skillVersion`，页面任务卡展示技能名称、版本、风险、影响对象和动作预览；确认后才调用对应 Skill 的 `apply`，取消不写入，仍不允许自动生成视频或扣费。已新增/更新单元测试覆盖 skill registry、按 Agent kind 生成多个 Skill 任务、任务 skill 追踪字段和确认应用回调；仍需真实页面复测：资产管理员一次生成两个 Skill 任务、提示词工程/分镜导演分别生成对应 Skill 任务、任务卡显示技能名称和版本、取消不写入、确认后写入仍可追溯。
- M5.9 AI 短剧 Agent 工作台第一版已实现，版本提升到 `v0.2.14`：项目详情页新增统一“Agent”入口，进入 `/projects/:id/agent` 后可在同一个任务中心中运行资产管理员 Agent、提示词工程 Agent、分镜导演 Agent。新增本地 `AgentTask` 数据结构和 store，使用 `localForage` key `infinite-canvas:agent_task_store` 保存 `id/projectId/kind/title/status/targetRefs/summary/riskLevel/proposedActions/createdAt/updatedAt`；第一版全部使用本地规则，不接入真实 LLM，不自动改画布、不自动生成视频、不自动扣费。资产管理员会读取当前项目素材、设定库和分镜，给出缺失素材、可复用素材、重复素材和待补标签建议；提示词工程会基于提示词仓库、设定库和分镜为缺提示词的分镜补草案；分镜导演会基于剧本分集/场次生成分镜组和条目草案。任务卡片展示建议、影响对象、风险和动作预览，用户取消不会修改数据，用户确认后才写入素材标签、分镜提示词或分镜组/条目，并保留 `agentTaskRefs` 或任务来源信息用于追溯。已新增纯函数测试覆盖三个 Agent 的建议生成；仍需真实页面复测：从项目详情进入 Agent 工作台、三个 Agent 生成任务卡、取消不写入、确认后分别补素材标签/补分镜提示词/从剧本创建分镜组、关闭重开后任务状态仍保存。
- M5.3.5 项目工作台总入口第一版已实现，版本提升到 `v0.2.13`：新增本地 `CreativeProject` 数据结构和 store，使用 `localForage` key `infinite-canvas:creative_project_store` 保存 `id/title/description/status/preset/coverAssetId/canvasIds/createdAt/updatedAt`；顶部导航新增“项目工作台”，`/projects` 支持新建、打开、重命名、归档和删除项目，新建项目复用 M5.3 画布预设能力。项目详情 `/projects/:id` 提供总览、画布、工作流入口和项目统计，可从当前项目新建画布并自动写入 `CanvasProject.projectId`，也可轻量绑定未归档旧画布；旧画布没有 `projectId` 时仍可从 `/canvas/:id` 直接打开，并在项目工作台中提示“未归档项目”。画布运行页已区分路由 `canvasId` 与业务 `workspaceProjectId`，新建的剧本、分镜、设定库、队列、生成素材 metadata 和分镜回流会优先使用 CreativeProject id，旧画布继续回退到画布 id。我的素材页新增项目上下文筛选并支持 `/assets?projectId=...`，会优先展示当前项目生成素材和设定库/分镜引用素材。已新增纯函数测试覆盖项目创建、更新、画布绑定去重和未归档旧画布识别；仍需真实页面复测：新建项目、从项目详情新建画布、绑定旧画布、打开项目内画布后剧本/分镜/设定库按同一 projectId 保存、从分镜打组加入画布后生成素材和回流仍落到同一项目、素材页项目筛选能同时显示生成素材和引用素材、归档/删除项目不会删除旧画布。
- 新建画布弹窗的预设字段已收口：`画幅` 改为 `画幅比例`，只显示横屏/竖屏/方形等比例选项，旧配置里类似 `3840x2160` 的像素尺寸会归一化为 `16:9`，不再把分辨率当画幅；默认图片/视频/文本模型从输入框改为下拉选择，选项来自当前 effective config 的分类模型列表。火山 Seedance 视频模型下拉会隐藏 `ep-...` Endpoint，只显示具体模型版本名，例如 `doubao-seedance-2-0-260128`；EP 继续作为后台渠道/本地配置里的请求绑定值，不在新建画布用户侧暴露。已用 Playwright 自动化复测通过：注入包含 `3840x2160` 和 `ep-20260605-test-hidden` 的本地配置后，打开新建画布弹窗不显示像素画幅，画幅下拉只出现比例选项，默认图片/视频/文本模型为下拉选择，OpenAI 与火山 Seedance 视频模型下拉均隐藏 `ep-...`，选择“竖屏短剧”后画幅为 `9:16` 且默认视频模型仍是具体 Seedance 模型。仍需真实生成链路复测：创建画布后后续视频生成仍能命中后台配置的企业 API Endpoint。
- 视频素材卡片、详情抽屉和画布“选择素材”弹窗已修复无封面展示：当视频素材没有单独 `coverUrl` 时，会用视频自身的首帧作为预览，不再显示文字占位。仍需真实页面复测：画布生成视频自动入库、本地导入视频、旧的无封面视频素材，在“我的素材”列表、详情抽屉和画布选择素材弹窗里都能显示视频首帧预览。
- 火山素材加白已扩展到视频参考：新增 `/api/v1/volcengine/assets/media-review`，并兼容旧的 `/api/v1/volcengine/assets/video-review` 视频加白入口，图片沿用原校验，视频会上传到公开素材 `videos/` 路径并以 `AssetType=Video` 调用火山 CreateAsset；“我的素材”中的视频卡片/详情、画布视频节点悬浮工具栏都支持“加白 / 刷新加白”，视频加白状态会在画布和“我的素材”之间同步。从“我的素材”或素材库插入视频到画布时会携带已有 `volcengineAsset`，Seedance 视频参考会优先提交 Active 状态的 `asset://...`；已提交但未 Active 的视频参考会在生成前本地阻断，并在 Ark 隐私审核失败时追加参考素材加白诊断。仍需真实页面复测：上传视频素材后提交加白、轮询到 Active、插入画布作为视频参考并确认请求走 `asset://`，以及 Processing/Failed 视频参考能否准确阻断并提示。
- 视频任务断网恢复已修复：视频节点拿到 `taskId` 后，如果轮询或视频回填时断网/超时/临时服务错误，不再把节点改成最终失败，而是保留为可恢复状态；重新联网、重新打开画布或恢复轮询后会继续按 `taskId` 查询任务，任务完成后自动下载并回填视频。仍需真实页面复测：生成视频拿到 taskId 后断网，等待上游生成完成，再联网确认节点能自动变成完成视频；旧的带 taskId 但被标成失败的断网页面节点也应能恢复。
- 视频节点顶部信息已归拢：左上角“任务详情 / 提示词”改为图标按钮，任务状态、URL 过期时间、本地缓存大小不再多标签常驻，常驻状态收为一条短状态胶囊，例如“完成 · 本地 3.0 MB”；URL 过期时间继续保留在任务详情里。仍需真实页面复测：视频节点在窄宽度和宽节点下顶部信息不遮挡人物主体、下载按钮和播放控制条。
- 视频节点底部原生播放控制条不再被任务状态徽标遮挡：任务状态、URL 过期时间和本地缓存大小徽标已从播放器底部移到顶部工具区下方，避免覆盖播放进度条、时间线和控制按钮。仍需真实页面复测：不同尺寸视频节点、选中/未选中、有无任务详情按钮时，原生控制条都可正常点击拖动。
- 火山 Seedance 视频生成新增参考图加白诊断：图片引用会携带 `volcengineAssetId/status`，如果参考图已提交加白但状态仍不是 `Active`，会在本地直接提示具体哪张图仍在 `Processing/Failed`，不再继续提交给 Ark；如果 Ark 仍返回 `InputImageSensitiveContentDetected.PrivacyInformation`，失败信息会追加“本次参考图加白诊断”，列出哪些图片没有以 `asset://` 加白素材提交。仍需真实页面复测：多张参考图中 1 张未 Active 时是否准确指出图片名；全部 Active 后请求是否不再因同一组参考图触发隐私信息错误。
- 视频失败节点的提示词展示已收口：选中节点并打开下方提示词面板时，失败卡片不再重复展示完整提示词，只保留错误和任务信息；未打开提示词面板时仍可在失败卡片中看到提示词，详情里也保留提示词行。仍需真实页面复测：失败节点选中/未选中、详情展开时提示词不重复且可读。
- 火山人像加白状态已在画布与“我的素材”之间同步：从“我的素材”插入图片到画布时会带上 `sourceAssetId` 和已有 `volcengineAsset`；画布里提交/刷新加白成功后会回写对应素材，若没有明确来源素材则按现有素材去重逻辑合并；视频生成前会用“我的素材”的最新加白状态补齐画布图片节点，确保 Active 素材能提交为 `asset://...`。仍需真实页面复测：在画布加白后打开“我的素材”能看到同一图片状态更新；在“我的素材”加白后插入画布生成视频，不再因该图片被当作未加白普通参考图而触发 `InputImageSensitiveContentDetected`。
- 视频生成失败后的计时已改为终态冻结：失败、取消、完成等终态会优先使用 `taskUpdatedAt` 等结束时间计算耗时，不再进入失败态后继续每秒增长；新的视频创建失败和重试失败写入节点时会同步记录失败时间。仍需真实页面复测：火山创建任务前失败、审核失败、任务失败后节点“已用”时间是否停在失败时刻，等待数分钟后不继续增加。
- 视频生成失败后的关系和提示词展示已修复：本次生成创建的普通 `输入节点 -> 失败视频节点` 连线会在失败后移除，失败视频节点保留自身 `metadata.prompt` 并在失败面板和详情中直接展示完整提示词，方便修改和重试。仍需真实页面复测：火山创建任务前失败、审核失败、超时失败后是否断开本次连线，且原提示词仍可见。
- 我的素材卡片仍保持统一 `4:3` 预览尺寸，但图片/视频/音频会在预览角落和详情内容区展示真实媒体信息：图片/视频显示实际宽高、文件大小和 MIME 类型，音频显示文件大小和 MIME 类型。仍需真实页面复测：不同尺寸图片导入后卡片预览统一，但信息显示原始尺寸而不是预览尺寸。
- 后台“系统设置 / 私有配置 / 新增渠道”已为火山方舟 Ark 渠道新增“Seedance Endpoint / EP”输入：填写 `ep-...` 后保存会自动合并进渠道可用模型 `models[]`，编辑已有渠道时会从模型列表回填第一个 `ep-...`。仍需真实页面复测：新增 Ark 渠道填写 EP 后保存、公开配置可选择该 EP 作为默认视频模型、云端视频生成能命中企业 API Endpoint。
- 视频节点失败态详情可读性已增强：失败原因不再只显示两行小字，错误正文和“详情”面板都放大为可滚动区域，并在详情行里展示完整错误。仍需真实页面复测：视频创建失败、火山审核失败、任务超时三类失败状态下，节点内错误是否能完整阅读且不触发画布缩放。
- 此前自检已修复画布“分镜”抽屉关闭状态下仍同步空素材选择导致的 React `Maximum update depth exceeded`，并把画布/后台相关 Drawer 的 `width` 属性改为 antd v6 推荐的 `size`；当时已在真实画布页面复测“设定库 / 剧本 / 分镜”三个入口可打开，前端终端未再出现新的循环报错或 Drawer `width` 警告。设定库入口现已按 M5.4 迁移到“我的素材”，仍需后续人工按新入口继续复测新增、编辑、保存、关闭重开后的数据恢复。
- 生成后的图片节点和视频节点已新增“提示词”入口：节点左上角可展开查看本次生成保存的完整 `metadata.prompt`，长提示词区域可滚动并阻止画布缩放；视频节点保留原“任务详情”入口。仍需真实页面复测：图片生成、视频生成、视频变体/编辑/延长完成后都能看到对应提示词。
- 后台侧栏“前往画布”导航行为已修复：移除按钮上的新窗口打开设置，点击后应在当前窗口跳转到 `/canvas`，不再新建网页或标签页。仍需真实页面复测后台点击“前往画布”后是否复用当前窗口。
- M5.8.2 批量生成队列与预算控制第一版已实现，版本提升到 `v0.2.12`：新增本地生成队列 store，使用 `localForage` key `infinite-canvas:generation_queue_store` 保存 `GenerationQueueItem` 和并发设置；第一版只支持从分镜组创建视频生成队列，图片/chat 仅保留 `kind` 预留。分镜抽屉中新增轻量“生成队列”面板，可在开始前看到预计视频数量、预计总时长、预计点数和缺失项检查；用户需要先“创建队列”并明确点击“开始队列”才会执行。队列支持默认并发 1，可设置并发数，支持开始、暂停、继续、取消、失败单项重试和只重试失败项；执行时复用现有单个视频配置节点生成逻辑，成功后继续走 M5.1 自动入库和 M5.8.1 分镜结果回流，失败项保留错误信息。已新增纯函数测试覆盖队列创建、缺失项检查、预算统计、状态流转和失败重试；仍需真实页面复测：分镜组打组加入画布后创建队列、预算/缺失显示、开始/暂停/继续/取消、单项失败重试、重试失败项，以及队列成功后素材和分镜结果回写。
- M5.8.1 分镜生成结果回流已实现，版本提升到 `v0.2.11`：当画布视频节点带有 `storyboardGroupId/storyboardShotId` 时，视频生成开始会把对应 `StoryboardShot.status` 更新为 `generating`，并记录结果视频节点 ID / taskId；视频生成成功并自动入库到“我的素材”后，会把素材 ID 写入 `resultAssetIds`，同一素材不会重复写入，首次成功结果会自动设置为 `primaryAssetId`。如果生成失败，会把分镜条目标记为 `error` 并保存 `errorMessage`；分镜抽屉现在展示生成结果列表、主版本、生成状态和失败原因。已新增纯函数测试覆盖生成中状态、成功回流、重复结果去重、首次主版本和失败原因；仍需真实页面复测：从分镜组打组加入画布后生成视频，确认分镜条目状态流转、素材回写、主版本显示、失败提示，以及重试同一分镜不会重复写入同一素材 ID。
- M5.7 分镜管理第一版已实现，版本提升到 `v0.2.10`：新增项目级分镜本地 store，使用 `localForage` key `infinite-canvas:storyboard_store` 保存 `StoryboardGroup` 和 `StoryboardShot`；画布工具栏新增“分镜”入口，可新增、编辑、删除分镜组和分镜条目，分镜条目支持上下排序、引用“我的素材”中的图片/视频/音频、引用 M5.4 设定库中的角色/场景/道具，并可从 M5.5 提示词仓库插入视频/正向词/工作流模板。剧本管理里的场次/分集“生成分镜草案”已升级为打开或创建真实分镜组，并回写 `ScriptScene.storyboardGroupId`。分镜组支持“打组加入画布”，会创建文本提示词节点、参考素材节点和视频生成配置节点，并在节点 metadata 写入 `storyboardGroupId/storyboardShotId`；分镜条目会记录创建出的 `nodeRefs`，本轮不做生成结果自动回流、不接入 Agent、不改后端。已新增单元测试覆盖分镜组/条目排序、引用归一化、从剧本场次创建分镜组、打组加入画布节点规划；仍需真实页面复测：分镜组和条目增删改、素材/设定引用保存、提示词模板插入、从剧本场次打开已有分镜组、打组加入画布后节点和连线正确、关闭重开后本地数据恢复。
- M5.6 剧本与集数管理第一版已实现，版本提升到 `v0.2.9`：新增项目级剧本本地 store，使用 `localForage` key `infinite-canvas:script_store` 保存 `ScriptProject`、`ScriptEpisode` 和 `ScriptScene`；画布工具栏新增“剧本”入口，可管理当前项目故事大纲、分集、场次、对白草稿、情绪节奏和预计时长。分集支持新增、编辑、删除和上下排序；场次支持新增、编辑、删除和上下排序，可引用 M5.4 设定库里的角色设定，并可选择场景设定回填地点。支持从纯文本按段落导入场次草稿；“生成分镜草案”已在 M5.7 升级为创建或打开真实分镜组。已新增单元测试覆盖分集/场次排序、场次归一化和纯文本导入解析；仍需真实页面复测：创建分集和场次、引用设定库、排序、文本导入、关闭重开后数据恢复、从场次进入分镜组。
- M5.5 提示词仓库完善第一版已实现，版本提升到 `v0.2.8`：后端 `prompts` 表新增 `metadata` JSON 字段，旧提示词没有 metadata 时继续按普通提示词展示；metadata 支持 `nodeGroup/type/scenario/provider/model/inputKind/outputKind/variables/favorite`，节点分组覆盖 `text/image/video`，用途覆盖 `asset/image/video/grid/positive/negative/workflow`。后台提示词管理可维护这些结构化字段和变量说明；前台提示词中心支持按节点分组、用途、场景、标签、常用筛选。画布提示词库弹窗已新增“新建”入口，从文本、图片、视频节点打开时会默认进入对应节点分组，正向词、负向词、工作流等作为用途筛选展示。提示词详情会展示变量说明和变量填写区，支持 `{角色}`、`{场景}`、`{镜头}`、`{时长}`、`{比例}` 等模板变量替换为最终提示词；在画布项目中打开提示词库时，可从当前项目设定库选择角色、场景、道具填入变量。已新增单元测试覆盖变量提取、变量替换、变量说明解析/格式化和设定库填充；仍需真实页面复测：画布弹窗新建提示词、按节点分组筛选、后台新增模板并保存 metadata、前台筛选、变量填写、设定库填充、视频创作台/画布视频节点插入最终提示词。
- M5.4 角色场景设定库第一版已实现，版本提升到 `v0.2.7`：新增项目级设定库本地 store，使用 `localForage` key `infinite-canvas:production_bible_store` 保存角色、场景、道具设定；每个设定项包含 `id/projectId/kind/name/description/tags/assetRefs/promptSnippets/createdAt/updatedAt`。入口已从画布工具栏迁移到“我的素材”的项目管理区，可先选择画布项目再打开“项目设定库”，在当前项目内新增、编辑、删除设定，支持从“我的素材”绑定图片、视频、音频或文本素材，并为每个绑定素材设置用途角色；提示词片段支持正向、反向和一致性。生成素材 metadata 已预留 `generation.productionBibleRefs`，本轮不接入生成流程、不自动拼接提示词、不改后端和素材存储 key。已新增单元测试覆盖设定输入归一化、项目筛选排序、类型/素材用途中文标签；仍需真实页面复测：“我的素材”按项目切换并打开设定库、画布底部不再显示设定库入口、三类设定增删改、素材绑定 role 保存、关闭重开后本地数据仍在。
- M5.3 画布项目预设已实现，版本提升到 `v0.2.6`：`CanvasProject` 新增可选 `preset` 字段，包含分辨率、画幅、帧率、默认时长、默认图片/视频/文本模型和默认视频供应商；新建画布弹窗支持从当前 effective config 带入默认值，也可选择“竖屏短剧 / 横屏短片 / 方形社媒 / 高清横屏 / 高清竖屏”常用预设后微调。新建配置节点、视频生成和生成素材归档都会读取当前画布 preset；旧画布无 preset 时继续按现有全局配置逻辑工作。生成素材的 `generation.config.projectPreset` 会记录来源项目 preset。已新增单元测试覆盖 preset 构建、配置套用、摘要和素材归档记录；仍需真实页面复测：新建不同预设画布后配置节点默认画幅/时长/模型正确，旧画布正常生成，素材详情里能看到 `projectPreset`。
- M5.2 素材详情增强已实现，版本提升到 `v0.2.5`：我的素材图片/视频详情抽屉新增“生成信息”区域，会读取 `metadata.generation` 与 `metadata.generations`，展示来源、画布项目、来源节点、提示词、实际提交提示词、模型、供应商、taskId、生成方式、生成时间、生成参数 JSON 和引用素材信息；视频 `actionType` 支持 `generate/variant/edit/extend/continue` 中文展示。详情里支持复制原提示词、实际提交提示词和生成参数 JSON；旧素材没有生成记录时显示“暂无生成信息”。素材页现有筛选区新增来源、生成方式、模型/供应商、有无 taskId 筛选，关键词搜索也会覆盖生成信息字段。已新增单元测试覆盖生成记录读取、中文 action label、筛选匹配和生成信息搜索文本，仍需真实页面复测：旧素材兼容显示、新生成图片/视频详情展示完整 metadata、复制按钮可用、四组新筛选符合预期。
- M5.1 生成素材自动归档已实现：画布图片生成成功、视频生成成功后会静默调用 `addAssetOnce` 自动加入“我的素材”，失败节点不会入库且仍保留原失败状态；图片素材 metadata 会记录 `generation.source/projectId/projectTitle/nodeId/prompt/effectivePrompt/model/provider/actionType/references/config/createdAt`，视频素材 metadata 额外记录 `taskId`，并预留 `storyboardGroupId/storyboardShotId`，`actionType` 会尽量区分 `generate/variant/edit/extend/continue`。已新增纯函数测试覆盖图片生成 metadata、失败不入库、视频任务和引用 metadata、视频动作类型归一化；仍需真实页面复测：画布生成图片/视频完成后“我的素材”自动出现且重复生成同一媒体不会重复新增。
- M5.0 “我的素材”去重写入底座已实现：`useAssetStore` 新增 `addAssetOnce`，图片、视频、音频素材会优先按 Blob SHA-256 指纹去重，取不到 Blob 时按 `storageKey + bytes + mimeType` 兜底；重复素材不会新增条目，会合并 tags、sourceRefs、generation/generations 和更新时间。生图工作台、视频创作台、素材库加入素材、我的素材手动新增/导入、画布节点保存/上传和画布包导入已改为优先走去重写入；文本素材保持原写入方式。已新增单元测试覆盖 Blob 指纹、兜底指纹、新素材 metadata 和重复素材 metadata 合并，仍需在真实页面复测：重复加入同一图片/视频/音频时列表不新增重复卡片，原素材标签和来源信息会更新。
- 代码结构收口 P1-P4 已完成：`/assets` 素材页拆出私有卡片、详情抽屉和素材工具函数，主页面格式门禁恢复；主画布页拆出剪贴板 hook，并新增纯函数测试覆盖复制选中节点、内部连线复制、粘贴居中和连线 ID 重映射；README、部署说明、docker-compose 和桌面包元数据已移除会指向原始仓库/镜像的对外残留，docker-compose 默认本地构建 `blink-workbench:local`；Ark 视频任务响应解析改为 typed DTO，并新增嵌套 `data.output[]` 返回格式测试。仍需在真实页面复测 `/assets` 素材卡片/详情抽屉、画布复制粘贴/系统剪贴板粘贴，以及重新打包桌面端后确认包名、数据目录和日志路径符合“眨眼之间”。
- 提示词管理已移除全部内置远程提示词源：后端分类列表只保留“系统”分类，启动时会清理旧版 GPT Image / GPT-4o / Nano Banana Pro 等远程内置分类下的提示词记录，后台“同步”按钮在没有远程分类时不再显示；管理员后续仍可手动新增、编辑、删除 `system` 分类提示词。当前本地 `data/infinite-canvas.db` 的 `prompts` 表已重建为空表，完整性检查为 `ok`，旧损坏库已备份到 `data/backups/infinite-canvas.db.malformed-prompts-20260603-143622`，另有操作前备份 `data/backups/infinite-canvas.db.before-remove-builtin-prompts-20260603-143400`。仍需重启后端并打开后台“提示词管理”和前台“提示词中心”确认内置提示词为空、同步入口不可见、手动新增提示词可正常保存和展示。
- 品牌展示已从“无限画布”调整为“眨眼之间”，并替换 `web/public/logo.svg` 为眼睛与瞬间闪光风格的图标；已同步 Web 标题、顶部导航、首页、登录页、管理后台侧栏、画布库标题、默认画布项目名、导出文件名，以及桌面端窗口标题、安装包产品名和快捷方式名。内部 localforage key、数据库名、GitHub 仓库名等兼容性标识未改。仍需重新 build/restart 后在真实页面复测 logo 显示、导航宽度、首页标题、登录页图标、管理后台侧栏和桌面安装包文件名。
- v0.2.2 工具布局收口：视频创作台 `/video` 的参数区改为单列稳定布局，Seedance 显示名/Endpoint 输入使用 `Space.Compact` 替代已废弃的 `Input addonBefore`，底部“开始生成”固定在参数面板底部；提示词库/素材、剪切板/上传按钮在窄列中保持两列稳定布局。我的素材 `/assets` 卡片底部操作改成图标按钮，避免查看/编辑/下载/加白/删除挤成碎行；顶部版本按钮增加固定高度、最小宽度和 nowrap，避免版本文字轻微溢出。仍需在真实页面复测 `/video`、`/assets` 和顶部导航。
- Gemini 图片模型的图像编辑请求已改为走 `/chat/completions` 多模态消息，不再走 `/images/edits`；返回解析支持 chat completion 中的 `image_url`、`b64_json`、data URL 和 markdown 图片地址，需用 `gemini-*-image-preview` 带参考图复测。
- Windows 11 x64 本地版 v0.2.0 安装包已重新打包：`desktop:prepare:win`、`desktop:dir:win`、`desktop:dist:win` 脚本已固定使用 `--x64`，避免 Apple Silicon 上误用 `arm64` 导致 Windows 打包失败；已执行 `cd web && npm run desktop:dist:win`，生成 `web/release/无限画布-0.2.0-Setup-x64.exe`，SHA256 为 `be521c6375bbecabc4616f1aaf8bff513257611b84aff229f46e2ef5e30825b4`。本机包体检查通过：安装器为 NSIS Windows 可执行文件，`win-unpacked/无限画布.exe` 和内置后端 `resources/bin/server.exe` 均为 Windows x64 PE；`resources/manifest.json` 指向 `web/server.js` 与 `bin/server.exe`，默认端口为 `3130/8180`；`resources/web/node_modules/next/package.json`、`resources/web/server.js` 和 `resources/web/.next/static` 均存在，避免此前 Next standalone 依赖缺失导致启动失败。当前是在 macOS 上构建和静态验包，未在真实 Windows 11 机器上执行安装/启动，仍需后续在 Win 11 上确认安装、启动、登录、页面打开、数据目录和退出清理。
- macOS 本地版 v0.2.0 打包与安装验收已完成：已执行 `cd web && npm run desktop:dist:mac:arm64`，生成 `web/release/无限画布-0.2.0-arm64.dmg`；`hdiutil verify` 校验通过，SHA256 为 `fbda30a7caf8e4c544b39ea14a0f4f3e293d1353aa9ef840edfd32182cfa60d0`，复制安装出的 `.app` 版本号为 `0.2.0`。安装版启动后，日志确认 Electron 自动从 `.app/Contents/Resources/bin/server` 拉起内置 Go 后端，并用 Electron 内置 Node 启动 `.app/Contents/Resources/web/server.js`，默认端口 `8180/3130` 均监听成功。页面验收通过：`/login`、`/canvas`、`/assets`、`/asset-library`、`/admin/settings`、`/video` 均返回 200；登录 API 返回 token，`/api/auth/me`、`/api/admin/settings`、`/api/assets` 和 Next 代理的 `/api/settings` 均返回 200。本地数据落点验收通过：运行中后端进程 cwd 为 `~/Library/Application Support/infinite-canvas-desktop`，实际打开的 SQLite 为 `~/Library/Application Support/infinite-canvas-desktop/data/infinite-canvas.db`，不是项目源码目录；关闭 App 后，`3130/8180` 无监听进程，日志记录后端和 Next 子进程退出。签名/Gatekeeper 记录：当前包为 ad-hoc 签名，`codesign --verify --deep --strict` 通过，但 `spctl --assess --type execute` 返回 `rejected`，未做 Apple Developer ID 签名和 notarization；通过微信或浏览器分发后仍可能触发隔离/“已损坏”提示。验收注意：如果旧版 App 已占用 `8180`，新实例可能出现后端端口冲突，验收前需先关闭旧实例。
- M4.4.1 云端/本地模式边界运行态验收已完成：已执行 `cd web && npm run build`，并以 Next standalone 重启 3000、Go 后端重启 8080；`3000 /api/health` 和 `8080 /api/health` 均返回 `ok`，真实页面可见版本 `v0.2.0`。管理后台 settings 验收通过：临时写入测试渠道后，已保存 API Key 在后台响应中只显示 `********`，响应 JSON 未出现真实 Key；保存掩码值、再保存空 API Key 后，云端请求仍能命中后端私有 Key，说明掩码/留空不会覆盖真实 Key。`allowCustomChannel=false` 页面验收通过：用户端配置弹窗只显示“云端渠道”，不显示本地 Base URL / API Key / Ark API Key 输入区，本地直连入口不可见；带有浏览器本地残留 Key 的云端 `/api/v1/videos` 请求仍走后端 private settings 中的 Ark 渠道。`allowCustomChannel=true` 页面验收通过：配置弹窗恢复“本地直连 / 云端渠道”选择，并显示 OpenAI Base URL/API Key 与 Ark API Key 输入区。云端代理安全验收通过：请求体注入 `_volcengine_api_key/_volcengine_base_url`、请求头注入 `X-Volcengine-*` 和 `X-Ark-*` 均未影响云端渠道，上游 mock 只收到后端私有 Key；本地模式下带本地 `_volcengine_api_key/_volcengine_base_url` 的请求只使用浏览器本地 Key，且不新增云端 `ai_tasks`。AI 任务日志安全复测通过：云端任务详情未出现后端 Key、前端注入 Key、token、`base64`、`data:` 或 `blob:` 内容。测试结束后已恢复原始后台 settings。
- M4.4 云端/本地模式边界加固已实现，版本提升到 `v0.2.0`：个人本地版边界保持不变，`allowCustomChannel=true` 时用户仍可选择本地直连并使用浏览器本地保存的 OpenAI/火山 Base URL 与 API Key，本地直连不走云端 ai_tasks 账本、不扣云端额度；云控商业版边界已加固，`allowCustomChannel=false` 时前端有效配置强制为 remote，配置弹窗不展示本地 Base URL/API Key 输入区，云端 `/api/v1/*` 视频代理会忽略浏览器残留的 `_volcengine_api_key` / `_volcengine_base_url` 和本地火山查询头，统一使用后端 private settings 中的供应商 Key。管理后台 settings 返回已保存模型渠道 API Key 时只显示掩码，保存时掩码或留空都会沿用旧密钥，不会把真实 Key 下发给前端。已新增单元测试覆盖：关闭自定义渠道后前端不能切到 local、remote 请求头/视频 payload 不携带本地 Key、本地模式仍使用本地 Key、后台 settings 不泄露真实 Key 且掩码保存不覆盖旧 Key、云端 Ark 视频代理不使用前端传入的供应商 Key。仍需后续在真实页面复测：管理员关闭/开启“允许自定义渠道”后配置弹窗显示是否正确、云端生成是否写入 ai_tasks 并扣云端额度、本地直连是否仍不写入云端账本。
- M4.3.2 管理后台页面主体不渲染问题已修复并完成页面验收：根因定位为 Next.js standalone 启动时缺少 `.next/static`，导致 `/_next/static/chunks/...` 的 JS/CSS chunk 返回 404，页面只能渲染服务端页头和 `v0.1.8`，登录页/后台页主体无法完成客户端 hydration。已新增 standalone 资源准备脚本，并把 `npm start` 改为先复制 `.next/static` 和 `public` 到 `.next/standalone` 后再启动 `server.js`；版本提升到 `v0.1.9`。重新 `npm run build` 通过，已重启 3000 standalone 和 8080 后端；`3000 /api/health`、`8080 /api/health` 均返回 `ok`，关键 JS/CSS chunk 从 404 恢复为 200，页面可见版本 `v0.1.9`。真实页面复测通过：登录页主体可见；管理员登录后 `/admin/ai-tasks` 后台侧栏可见，侧栏包含“AI 任务日志”；列表、分页、关键词、用户、状态、类型、动作、模型、渠道、上游 taskId 和时间范围筛选入口可见并可用，其中关键词/用户/模型/渠道/上游 taskId/状态/类型/动作已逐项页面查询返回正确任务。详情抽屉可见任务基础信息、用户、请求/响应 JSON、额度流水、错误、视频 URL 过期时间、完成时间、返还时间；刷新状态按钮可见并在无匹配 Ark 渠道时提示“未找到可刷新该任务的 Ark 渠道”；新增独立页面验收任务 `aitask-m432-refund` 后，手动返还按钮可点，确认后返还数从 0 变为 2，重复返还按钮变为禁用。安全复测通过：详情中未发现真实 API Key、token、secret、完整 base64、`data:` 或 `blob:` 长内容，只显示 `[redacted]` / `[media redacted]`。仍需后续用真实 Ark 渠道任务复测“刷新状态”成功路径。
- M4.3.1 管理后台 AI 任务日志验收记录：已重新 `npm run build`，并先后重启 3000，最终按项目 standalone 方式运行；由于 8080 旧进程仍是 M4.2 前代码，验收中同步重启了后端，启动日志确认 `/api/admin/ai-tasks`、`/api/admin/ai-tasks/:id`、`refresh`、`refund` 路由已注册。`3000 /api/health` 和 `8080 /api/health` 均返回 `ok`，页面页头可见版本 `v0.1.8`。默认管理员账号登录接口可用。真实库原本没有 `ai_tasks`，为验收插入了独立测试用户 `user-m431-acceptance`、任务 `aitask-m431-acceptance` 和关联消费流水；后台接口验证通过：列表加载、分页、用户/状态/类型/动作/模型/渠道/上游 taskId/时间范围/关键词筛选均返回正确结果；详情接口返回任务基础信息、用户简要信息、脱敏 `request_json/response_json`、关联 `credit_logs`、错误信息、视频 URL、过期时间、`finishedAt/refundedAt`；安全检查未发现真实 API Key、token、secret、完整 base64、`data:` 或 `blob:` 长内容。手动返还第一次成功写入 `ai_refund`，测试用户余额从 7 回到 10；第二次返还返回“任务已返还，不能重复返还”。这条验收任务没有匹配的真实 Ark 渠道，点击/调用刷新状态返回“未找到可刷新该任务的 Ark 渠道”，真实 Ark 刷新仍需后续用实际任务复测。
- M4.3.1 页面可见 UI 未通过：在 Chrome 和 Codex 内置浏览器打开 `localhost:3000/login?redirect=/admin/ai-tasks` 时，页头和 `v0.1.8` 能显示，但登录/后台主体区域未挂载，只看到顶部导航，无法完成侧栏“AI 任务日志”、`/admin/ai-tasks` 列表、详情抽屉、筛选控件和按钮的真实可见 UI 操作验收。当前结论是接口和数据链路已通过，但管理后台页面主体渲染需要单独排查后再做页面级复测。
- M4.3 管理后台 AI 任务日志已实现：新增管理员接口 `GET /api/admin/ai-tasks`、`GET /api/admin/ai-tasks/:id`、`POST /api/admin/ai-tasks/:id/refresh`、`POST /api/admin/ai-tasks/:id/refund`；列表支持用户、状态、类型、动作、模型、渠道、上游 taskId、时间范围、关键词和分页筛选；详情返回任务、用户简要信息、关联 `credit_logs`、脱敏请求/响应、错误、视频 URL、过期时间、返还和完成时间；刷新仅对 Ark 视频上游任务开放并复用 M4.2 状态同步和幂等返还；手动返还会检查 `refunded_at`、`credits_refunded` 和已有返还流水避免重复。后台新增 `/admin/ai-tasks` 页面和导航入口，可查看列表、详情抽屉、刷新状态和手动返还。后端测试已覆盖列表筛选、详情、视频刷新、失败手动返还和重复返还拦截，仍需后续在真实管理后台页面用管理员账号确认筛选、详情抽屉和按钮交互。
- M4.2 视频异步任务生命周期与失败返还已实现：查询 `/api/v1/videos/:id` 时会按 `upstream_task_id` 同步更新 `ai_tasks` 的 `status/raw_status/response_json/video_url/video_url_expires_at/error_code/error_message`；`queued/running/succeeded` 不返还，`failed/cancelled` 会把任务标失败并按 `refunded_at` 幂等返还一次；重复刷新失败任务不会重复加回算力点；`/api/v1/videos/:id/content` 成功下载后会记录 `finished_at`。后端测试已覆盖 running 不返还、failed 返还一次、重复 failed 不重复返还、succeeded 不返还和 content finished_at，仍需后续用真实 Ark 长任务页面确认数据库状态随轮询变化。
- M4.1 AI 任务账本基础已实现：新增 `ai_tasks` 表，后端云端 AI 代理的生图、图生图、聊天和视频创建会创建任务记录；扣费和失败返还的 `credit_logs.related_id` 会指向 `ai_tasks.id`；上游同步失败会把任务标记为失败并返还；Ark 视频创建成功会写入 `upstream_task_id` 和上游初始状态；请求/响应账本会脱敏 API Key、base64、blob URL 和文件内容。后端单元测试已覆盖成功记录、失败返还关联和脱敏，仍需后续在真实后台云端渠道页面触发一次生图/聊天/Ark 视频创建，确认管理端数据库记录与实际扣费流水一致。
- 新增 Windows x64 桌面安装包封装：`web` 目录下可通过 `npm run desktop:dist:win` 构建 NSIS `.exe` 安装器；Electron 启动后会自动拉起内置 Go 后端和 Next.js standalone 服务，并把 SQLite、素材、提示词缓存和 JWT Secret 放到系统用户数据目录。
- 新增 macOS 桌面安装包封装：`web` 目录下可通过 `npm run desktop:dist:mac` 构建当前机器架构的 `.dmg`，也支持 `desktop:dist:mac:arm64` 和 `desktop:dist:mac:x64` 指定架构；运行数据同样放到系统用户数据目录。
- 修复 macOS 桌面端启动超时：打包后显式补回 Next.js standalone 的 `node_modules`，避免 Electron 内置 Node 启动 `server.js` 时找不到 `next`；桌面启动失败时会把后端和前端子进程日志写入 `desktop.log` 便于定位。
- macOS 桌面包改为 ad-hoc 签名，避免本地资源未封签导致的“应用已损坏”问题；通过微信或浏览器分发后仍可能被 Gatekeeper 隔离，正式对外分发需 Apple Developer ID 签名和公证。
- AI 代理接口会透传上游错误原因：火山 Ark、OpenAI 兼容图片/聊天/视频接口返回 4xx/5xx 时，节点失败提示优先显示上游 `error.code` / `error.message`，避免只看到笼统的“AI 接口请求失败”。
- 画布音视频服务端缓存接口改为登录后可用；未登录时只保留浏览器本地缓存，不再允许匿名上传文件到公开缓存目录。视频下载代理会拒绝非 HTTP(S)、localhost、内网 IP、链路本地地址等目标，避免服务端代拉内部地址。
- 开发环境新增本地测试免输入：无 token、token 失效或停留在登录页时会自动尝试使用默认测试账号登录；AI 配置检查在开发环境默认放行，并支持通过 `NEXT_PUBLIC_DEV_*` 环境变量预填测试接口配置或关闭临时放行。
- Seedance 2.0 视频节点支持 P1 连续视频工作流：图片参考可在输入预览中指定为首帧、尾帧或普通参考图；火山请求会传入 `first_frame`、`last_frame` 和 `return_last_frame`；生成完成后如果 Ark 返回尾帧，会保存尾帧地址，只有在视频节点悬浮工具栏明确点击“续写”时，才创建“上一段尾帧”图片节点，并连接到一个空的“下一段视频”节点作为下一段首帧输入。
- 画布视频节点新增 M1 关系语义：已生成视频节点修改提示词后点击生成，会创建平行变体视频节点，不再自动创建串行下一段；变体节点 metadata 会标记 `actionType/videoActionType=variant`、`variantOfNodeId`，续写节点会标记 `actionType/videoActionType=continue`、`continuationOfNodeId`。
- 修复已完成视频节点重新生成变体时被错误画成串行下一段的问题：平行变体节点不再创建 `原视频 -> 新视频` 画布连线，位置改为原视频下方偏右；变体会继续记录 `relationType=variant`、`variantOfNodeId/sourceVideoNodeId`，但普通生成模式不会把原视频作为 `source_video` 提交，只有编辑和延长模式才使用源视频关系。
- 画布 Seedance 视频设置新增 M2 任务模式：生成、编辑、延长；编辑支持 `replace/add/remove/inpaint` 类型，延长支持向后/向前方向。编辑和延长会以上游视频节点作为源视频创建新的派生视频节点，metadata 标记 `actionType/videoActionType=edit|extend`、`relationType=derivative`、`sourceVideoNodeId`，Ark payload 会把源视频写为 `source_video` content，并分别带上 `task_mode/edit_type/extend_direction`。
- 修复 M2 编辑/延长引用已生成视频时优先拿本地 `blob:` 播放地址的问题；现在上游视频节点如果保存了 Ark 返回的 `videoUrl`，会优先作为 `source_video` 引用，避免把仅浏览器可访问的本地缓存地址传给 Seedance。
- M2.1 视频节点新增任务可观测界面：生成中、失败和已完成视频节点会在节点内展示阶段式进度、耗时、taskId、任务详情；有 taskId 时支持手动刷新任务状态并回填最新 Ark 查询结果，方便观察 Seedance 长任务处于创建、排队、生成、回填或完成阶段。
- 修复视频生成中切换页面再回来被误判失败的问题：进入画布时，带 `taskId` 的 loading 视频节点会保留任务进度并自动刷新 Ark 状态；任务仍 queued/running 时继续显示进度，succeeded 时会继续下载视频、写入本地缓存并更新为 success，failed/cancelled 才标记为 error；没有 `taskId` 的 loading 视频节点会显示“任务创建未完成，请重新生成。”。
- 视频节点新增“截帧”入口，可对任意已加载视频截取播放器当前时间点画面，不依赖 Ark `last_frame_url`；截取结果会作为“当前帧”图片节点创建在视频节点旁，写入本地图片存储并记录来源视频节点 ID、截取时间点和来源关系，可继续作为普通图片参考连接到视频配置节点。
- 视频节点提示词输入/预览体验优化：视频模式下提示词区域字号和行高提高，设置最大高度并支持长提示词纵向滚动；提示词区域标记为画布 no-zoom，滚轮只滚动提示词内容，不触发画布缩放或拖拽。
- M2.2 页面收口验收记录：已重新 `npm run build` 并重启 3000，3000/8080 健康检查均返回 `ok`；真实画布页面可见视频任务进度、耗时、任务详情、已完成视频的 URL 有效期和本地缓存大小。长提示词预览在页面中已确认固定高度、可滚动、`data-canvas-no-zoom` 生效，计算样式为 15px 字号和 24px 行高；M2.4 已通过真实滚轮复测确认“滚轮只滚动提示词、不缩放画布”。
- M2.2 仍需人工复测：当前页面已有 3 个视频节点加载为本地 `blob:` 播放源并达到 `readyState=4`，但浏览器自动化未能稳定唤出视频节点 hover 工具条，因此“截帧”按钮的播放中截取、暂停截取、接近结尾截取、生成图片节点、本地存储，以及截取图片连接到视频配置节点并设置为 `first_frame/last_frame/reference_image` 仍需人工在页面完成；平行变体、显式续写、编辑派生、延长派生的关系隔离已由单元测试覆盖，真实 Ark 页面生成链路仍需继续按实际素材复测。
- M2.3 页面关系回归验收记录：已重新 `npm run build` 并重启 3000，3000/8080 健康检查均返回 `ok`。真实画布 `rFZdaeZun7HyFTgGRnGiJ` 中，视频节点 `video-1780378747723-9k9cr` 只有图片参考连入，未发现 `video-1780378747723-9k9cr -> 新视频` 的直接画布连线，符合平行变体不创建 `CanvasConnection` 的规则；选中该视频后，长提示词面板仍为 15px / 24px、固定高度、可滚动并带 `data-canvas-no-zoom`。页面当前还存在两条旧的 `视频 -> 视频` 连线（`video-1780380595167-6-405in -> NbruQ1sKHwFdHPzcoSecA -> LzvdlQLmkoUFB_VpDBNtm`），浏览器自动化只能从 DOM 推断连接拓扑，无法直接读取这些旧节点 metadata 判定是显式续写还是历史旧数据，需人工打开节点信息确认 `relationType`。
- M2.3 仍需人工复测：需要人工在真实页面执行一次“已完成视频改提示词后生成”，确认新 B 节点 metadata 为 `relationType=variant` 且连接数不增加；需要人工点击“续写”确认只有该入口创建 `视频 -> 尾帧图 -> 下一段视频`；编辑 / 延长需要人工确认节点 metadata 为 `derivative` 且不覆盖源视频；截取当前帧、配置节点连接上游视频后作为 `source_video` 的页面操作仍需人工复测。
- M2.4 视频能力人工验收记录：真实画布 `rFZdaeZun7HyFTgGRnGiJ` 中，任务详情已能读到两个变体节点：`NbruQ1sKHwFdHPzcoSecA` 显示 `模式 generate / 关系 variant / 源视频 video-1780380595167-6-405in`，`LzvdlQLmkoUFB_VpDBNtm` 显示 `模式 generate / 关系 variant / 源视频 NbruQ1sKHwFdHPzcoSecA`，说明页面可观测层能展示 variant 关系和源视频 ID。长提示词滚轮已复测通过：提示词 textarea 带 `data-canvas-no-zoom`，滚轮后 `scrollTop` 从 0 变为 420，画布缩放仍保持 31%。
- M2.4 仍需人工复测/保留项：当前画布未发现可见的编辑/延长 `derivative` 完成节点，仍需确认 `relationType=derivative / sourceVideoNodeId`；本轮未发新的真实 Ark 最小 `source_video` 任务，配置节点连接上游视频后的 `source_video` 路径可用性仍保留；截取当前帧的播放中、暂停、接近结尾三种截取，以及截取图片连接到视频配置节点并设置为 `reference_image / first_frame / last_frame` 仍需人工页面复测。
- M2.5 历史变体连线清理已验收：新增加载时清理逻辑后，真实画布 `rFZdaeZun7HyFTgGRnGiJ` 里历史错误连线 `video-1780380595167-6-405in -> NbruQ1sKHwFdHPzcoSecA` 和 `NbruQ1sKHwFdHPzcoSecA -> LzvdlQLmkoUFB_VpDBNtm` 已被移除；页面连接数从 16 降到 13，`video -> video` 直连为 0，图片参考连到视频的连接仍保留。
- M2.5 当前新规则已验证：在 `v0.1.1` 真实页面选中已完成视频节点 `video-1780378747723-9k9cr`，修改提示词后点击“生成”，创建了新的变体视频节点 `pBuAlSmQQEtWDkfIWcZ9B`；节点数从 16 增加到 17，但连接数仍保持 13，`video -> video` 直连仍为 0，确认当前重新生成变体不会再新增 `源视频 -> 变体视频` CanvasConnection。该真实 Ark 任务已进入生成中，后续仅需按普通长任务继续观察最终回填。
- M2.6 Seedance 视频设置 UI 已收口，版本提升到 `v0.2.1`：视频设置中“任务模式”改为“生成方式”，选项改为“生成新视频 / 编辑视频 / 延长视频”；没有源视频时只展示“生成新视频”，有已完成视频节点自身或配置节点上游视频输入时才展示编辑/延长；旧配置如果在无源视频上下文中保留 edit/extend，会自动回落到 generate。原“参考图用途”改为“图片控制”，UI 中移除“续写”选项，仅保留“普通参考 / 作为首帧 / 首尾帧”；编辑/延长模式下隐藏图片控制。底层仍保留 `videoReferenceImageMode=continue` 兼容显式续写链路，未改 Seedance payload、source_video 请求逻辑或视频节点悬浮工具栏“续写”入口。已通过前端类型检查、全量单元测试和格式检查，仍需在真实页面复测上述显示条件。
- M3.1 画布助手动作协议基础纯函数已实现并进入页面链路：新增动作类型、validator、preview builder 和 apply helper，第一版仅允许 `canvas.read / canvas.summarize / node.explain_context / node.create_text / node.create_config / connection.create`；会拒绝删除节点、删除连线、覆盖结果、自动触发 AI 生成，`connection.create` 会校验节点存在、不能自连、不能重复连线。
- M3.2 画布助手动作预览已接入 `CanvasAssistantPanel` 并完成页面验收：助手消息可展示受控 `AssistantCanvasAction` 预览卡片，显示动作原因、将创建的节点/连线数量、影响节点/连线和风险校验；写入动作不会自动应用，用户点击“应用到画布”后才通过 `applyAssistantCanvasActions` 返回新的 nodes/connections 并触发现有画布历史，支持撤销；点击“取消”只更新助手消息状态，不修改画布。第一版只提供开发调试固定 action 入口，不允许模型自由生成任意 action。
- M3.3 画布助手只读能力已接入并完成页面验收：`CanvasAssistantPanel` 的“总结当前画布”可在真实页面生成中文摘要，读取节点类型、标题、提示词/内容、任务状态和连线关系；“解释选中节点”可列出当前节点、上游节点、下游节点，并标记参考、变体、续写、派生或普通连线。只读动作不会修改 nodes/connections，并通过跳过画布历史的 session 更新避免污染撤销栈。
- M3.4 画布助手自然语言转受控动作建议已接入并完成页面验收：助手输入会先用本地规则解析“创建文本节点”“创建视频/图片生成配置节点”“连接当前选中节点到新配置节点”“连接两个已选节点”等请求，解析结果统一转成 `AssistantCanvasAction`，顺序经过 validator 和 preview builder；页面只显示动作预览，用户确认后才应用，无法解析时回退到原有助手流程，不允许模型自由执行动作。
- M3.5 页面验收记录：已重新 `npm run build` 并重启 3000，真实页面显示 `v0.1.5`，3000/8080 健康检查均返回 `ok`。在画布 `rFZdaeZun7HyFTgGRnGiJ` 上验证通过：“总结当前画布”输出真实画布摘要；“解释选中节点”在多选两个图片节点后输出当前节点、上游/下游和普通连线关系；“创建一个文本节点，内容是M3.5 确认应用文本”先显示 1 个节点/0 条连线预览，取消不改变画布，确认后重新总结显示节点数从 26 增加到 27、连线仍为 13，点击撤销后恢复为 26/13；“帮我创建一个视频配置节点”和“帮我创建一个图片生成配置节点”均只显示 1 个配置节点预览，取消后仍为 26/13；“把选中的图片连到视频配置”在选中图片节点后显示 1 个配置节点/1 条连线预览，取消后仍为 26/13；“连接两个已选节点”在 shift 多选两个图片节点后显示 0 个节点/1 条连线预览，取消后仍为 26/13；无法解析的“你觉得这个故事怎么样”没有产生动作预览，重新总结仍为 26/13。页面未开放删除节点、覆盖节点或真实 AI 生成动作。
- M2 页面验收记录：当前 3000 重新 build 后，视频节点设置面板能显示“生成 / 编辑 / 延长”，编辑模式能显示 `替换 / 添加 / 移除 / 重绘`；在已完成视频节点自身的 prompt 面板上选择“编辑 + 替换”并生成，会创建新的派生视频节点，源视频节点保持已完成且未被覆盖，也没有自动创建“上一段尾帧 / 下一段视频”连续链路。浏览器自动化未能可靠拖拽完成“视频节点连接到配置节点”的连线场景，该路径仍需人工页面复测。当前 Ark 编辑任务页面轮询超过 5 分钟仍停留在“生成中”，因此最终视频回填、本地缓存、刷新后播放、失败态展示，以及 `add`、向后/向前延长的真实页面生成仍需复测；代码路径 payload 已确认会输出 `task_mode=edit|extend`、`edit_type` / `extend_direction` 和 `source_video`。
- 画布助手打开时不再显示“未开发”标记；选中生成配置等画布节点后，助手会根据连线自动把上游图片和文本节点纳入引用，引用顺序尊重节点的输入顺序设置。
- Seedance 2.0 视频节点新增 P2 多模态参考能力：视频生成配置节点会收集上游图片、视频、音频节点，输入预览中统一展示所有参考素材并允许跨类型调整顺序；图片参考可指定 `reference_image / first_frame / last_frame`，视频按 `reference_video`、音频按 `reference_audio` 提交；Ark 请求会按输入顺序生成 `content` 数组，并继续兼容 `return_last_frame` 和连续视频链路；视频提示词输入框支持输入 `@` 通过预览选择已连接的图片、视频或音频参考并插入 `图片 1 / 视频 1 / 音频 1` 官方标签。
- Seedance 视频节点新增“参考图用途模式”：默认“普通参考”会把所有图片作为 `reference_image`；用户在视频设置中明确选择“首帧”“首尾帧”或连续视频链路创建的“续写”时，才会自动生成 `first_frame / last_frame` 角色；输入预览中每张图片的角色下拉仍可作为高级手动覆盖。
- Seedance 视频设置的秒数改为连续 4~15 秒区间控件；画布视频配置节点调整供应商、模型、画幅比例、秒数、清晰度、音频、水印、seed 和图片控制后，会同步成为下一次新建视频配置节点的默认值，避免每次新建都重新调整。
- 视频设置面板按视频协议识别 Seedance，不再因渠道模式误判成 OpenAI 秒数结构；Ark 参数只在火山 Seedance 模式下展示，避免出现 OpenAI 秒数和 Ark 参数混在一起的界面。
- 画布视频节点和生成配置节点新增本地视频供应商切换，可在“OpenAI 兼容”和“火山 Seedance”之间按节点选择；切换时会使用对应默认视频模型，生成和重试会沿用节点保存的供应商与模型。
- 默认模型选择器改为可搜索弹窗，支持按模型名或厂商别名筛选；本地渠道可直接输入自定义模型 ID，管理后台公开配置里的默认模型下拉也按模型名检索。
- API 配置弹窗将 OpenAI 兼容和火山 Seedance 的 Base URL/API Key 拆开保存；默认视频模型可在“OpenAI 兼容视频”和“火山 Seedance”之间切换，Seedance 区分显示模型名和实际调用的 Endpoint ID。
- 火山方舟 Ark 配置改为预设 Seedance 接口地址，界面需要填写 Ark API Key 和 Seedance Endpoint ID；OpenAI 兼容和火山方舟配置在视觉上分成两个独立区块。
- OpenAI 兼容模型拉取后仍使用同一份完整模型列表，生图/视频/文本选择器都能检索到所有模型，避免按名称分类误伤；本地直连仍支持手动输入模型 ID。
- Seedance 视频设置新增 Ark 参数：`duration/ratio/resolution`，其中 `duration` 按 Seedance 2.0 要求以 4~15 秒整数提交，不再压成 5/10/15 档位；本地火山方舟请求和后端 Ark 代理都会传入 `generate_audio/watermark/seed`。
- 画布视频节点会展示更完整的任务信息：生成中/完成/失败状态、失败原因、`video_url` 有效期，以及视频转存到本地后的 `storageKey`、大小、类型和转存时间。
- 画布项目导出改为下载 `.zip` 压缩包，包内包含 `projects.json` 和当前画布引用到的本地图片、视频文件，避免只导出 JSON 时丢失媒体内容。
- 画布库支持多选后一键导出多个画布项目，导出的压缩包可一次恢复多个项目。
- 画布项目导入改为读取新版 `.zip` 压缩包，会先按 `projects.json` 中的文件映射恢复图片、视频到本地存储，再插入画布项目，导入成功后仍停留在画布库。
- 画布中通过菜单上传、拖拽、剪贴板粘贴或替换节点导入的图片、视频和音频，会自动加入“我的素材”；导入画布压缩包时也会把画布里的可保存节点同步加入“我的素材”。
- 修复删除画布图片节点或清空画布后撤销时，节点信息恢复但本地图片数据已被清理导致图片丢失的问题。
- “我的素材”类型筛选区右侧新增文本样式的导出素材和导入素材入口，可将全部素材导出为包含 `assets.json` 与图片、视频、音频文件的压缩包，并从压缩包恢复素材；导入入口也支持直接选择本地图片、视频和音频文件创建素材。
- “我的素材”新增直接拖拽上传图片、视频、音频和素材压缩包；当前选中文件夹会作为上传目标。
- “我的素材”新增本地文件夹管理，可新建、重命名、删除文件夹，按文件夹筛选素材，并在新增/编辑素材时选择所属文件夹；删除文件夹时素材会回到未分组。
- “我的素材”导出流程新增明确选择状态：素材卡片可逐个勾选，筛选结果可一键全选并清空选择，顶部会显示已选数量和已选素材名称；导出入口拆分为“导出选中”和“导出全部”，避免导出前不知道会导出哪些素材。
- “我的素材”新增本地视频、音频上传与预览，导入导出会一并保存音视频文件。
- 未登录状态下，画布右上角不再显示用户头像菜单、用户名称、算力点余额和退出登录入口，改为显示登录入口；快捷键入口仍可直接打开。
- 生图工作台的图片参数区复用画布里的紧凑版图像设置面板，尺寸、质量、生成张数的交互保持一致；工作台仍保留独立的模型选择。
- 生图工作台新增生成记录配置持久化：每次生成会保存提示词、参考图、模型、质量、尺寸和张数，结果图写入本地图片存储后记录只保存 `storageKey`；点击历史记录会回填本次生成配置并预览结果。
- 视频设置抽成画布和视频创作台共用的紧凑面板，清晰度、尺寸、秒数按固定网格选择并支持手动输入；尺寸选择 `auto` 时请求不传 `size`。
- 视频设置面板将“尺寸”改为 Seedance/Ark 实际使用的“画幅比例”，按钮摘要显示 `resolution + ratio + 秒数`；旧的 `1280x720/720x1280/1024x1024` 会自动映射为 `16:9/9:16/1:1`，旧的 `480p/low` 会按 Ark 能力提交为 `720p`，避免界面显示与 payload 不一致。
- 修复画布和生图工作台选择图片尺寸后，请求图片生成/编辑接口未携带 `size` 参数的问题；`auto` 不传，其余比例或像素尺寸会随请求发送。
- 修复生图工作台和画布生图请求中 `quality` 参数可能传入上游不支持值导致 400 的问题；请求前会归一化质量枚举，`auto` 或异常值不再发送给上游。
- 管理后台新增/编辑渠道时，渠道可用模型支持通过弹窗按“新获取、已有”分组选择，并可在弹窗内手动增加模型或拉取模型列表后再写回表单。
- 管理后台编辑渠道时，API Key 留空不再触发必填校验，表示沿用已保存的密钥；新增渠道仍要求填写 API Key。
- 管理后台公开配置里的系统可用模型候选项改为由已启用渠道中选择的模型合并去重生成，最终开放哪些模型仍由公开配置里手动勾选。
- 视频生成请求参数对齐 `grok-imagine-video` 接口：使用 `resolution_name`、`preset=normal`、`input_reference[]`，支持清晰度、尺寸、秒数快捷选择和手动输入，并支持最多 7 张参考图。
- 画布视频设置浮层改为挂载到页面根层级并使用自建浮层交互，避免被节点悬浮工具栏遮挡或点击面板内容时关闭。
- 画布生成配置节点的生图参数改为复用图像设置浮层，支持在同一个入口里调整质量、尺寸和生成张数。
- 视频清晰度输入框改为只输入数字，提交请求时再拼接 `p` 单位。
- 视频生成前端会识别后端 `{ code, msg }` 错误响应，创建失败不再继续轮询 `undefined`。
- 新增 `/video` 视频创作台页面，参考生图工作台布局，支持提示词、参考图、视频参数、生成结果、保存素材、下载和本地生成记录；清晰度和秒数均支持常用值选择与手动输入，生成记录只保存媒体 `storageKey` 并可回填本次提示词、参考图和参数。
- 视频创作台生成前会把模型、尺寸、秒数、清晰度归一化为视频接口支持的参数，并展示后端返回的错误信息，避免页面侧残留的生图参数影响视频请求。
- 后台“素材管理”新增图片、视频、音频文件上传，文件保存到后端公开静态目录并自动回填素材 URL；前台“素材库”支持筛选、预览并加入这些媒体素材。

## 火山私域人像素材加白

- “我的素材”的图片素材支持提交到火山私域虚拟人像素材资产库。
- “我的素材”图片卡片和详情抽屉中的“加白”按钮改为点击后直接提交审核，不再弹出二次确认。
- “我的素材”和画布图片节点的加白入口对图片始终可见；未开启火山人像加白配置时，点击会提示先开启配置。
- 画布图片节点悬浮工具条新增“加白/刷新加白”入口，可把节点图片直接提交到火山素材库，并把 Asset ID、素材组和审核状态写回节点信息。
- 画布 Seedance 视频生成会优先把已校验成功的图片参考作为 `asset://<Asset ID>` 提交，避免加白素材在生成视频时仍被当成普通图片 URL。
- 后端会先把本地图片保存为公开静态文件，再使用后台配置的 AK/SK、ProjectName 调用火山素材接口。
- 火山人像加白配置入口收口到后台“系统设置”的“火山素材审核（唯一配置入口）”；加白配置不同于 Seedance 生成渠道，填一次即可，本地直连/云端渠道只影响视频生成 Key。
- 前台右上角“配置”弹窗中的“火山人像加白”只读展示当前是否开启、ProjectName、Region、素材组 ID 摘要，并提供“去后台设置”提示，不再显示 AK/SK 输入。
- 后台“素材管理”页的“火山素材审核”只显示状态和“去系统设置”按钮，不再重复填写 AK/SK；素材列表仍只保留图片加白/刷新加白操作。
- 后台“素材管理”图片素材列表新增加白/刷新按钮，上传生成的 `/api/uploaded-assets/...` 地址会按公网素材访问地址转换后提交火山。
- 图片素材详情中可查看火山 Asset ID、素材组 ID、ProjectName，并可手动刷新审核状态。
- 需要配置公网可访问的素材地址；本地开发地址无法被火山服务拉取。
- 公网素材访问地址为火山 TOS 域名时，后端会在提交 `CreateAsset` 前把图片自动上传到对应桶路径，避免只拼出 TOS URL 但桶内对象不存在。
- 上传调用对齐火山官方 demo：配置素材组 ID 后会作为 `CreateAsset` 的 `GroupId` 直接传入；留空才自动新建素材组，上传和查询都使用配置中的 ProjectName。
- 素材本身已经是 `https://...` 公网地址时会直接提交；只有 `/uploaded-assets/...` 本地上传路径才需要通过公网素材访问地址转换。
- 提交审核或刷新状态时，如果火山返回接口错误或素材处理失败原因，会在页面提示和素材详情中显示具体错误。
- 公网素材访问地址会拒绝 `localhost`、`.local` 和内网 IP，避免把火山无法访问的本地地址提交给素材库。
- 图片格式校验对齐火山 `CreateAsset` 文档，除 jpeg/png/webp/bmp/tiff/gif 外，也允许 heic/heif，并会读取 HEIF 尺寸后再提交。
- 提交后的 `Processing` 火山素材会在“我的素材”和画布图片节点中自动轮询 `GetAsset`，按钮显示“自动刷新中/审核中”，状态变为 `Active` 或 `Failed` 后会自动更新素材标签、详情和节点信息，不需要手动点刷新。
