# 提示词五类业务分类进度

## 当前状态

- 当前阶段：6 / 已完成。

## 已完成

- 明确后台与外侧原本已共用同一 API 数据源。
- 建立设计说明与逐步实施计划。
- Go RED 测试确认旧实现只有 `system` 且种子未归类。
- repository 固定五类并为旧种子补齐分类，同时保留编辑内容。
- 前端增加共享五类定义、中文标签和颜色。
- 后台筛选和编辑改为必选业务分类，继续支持自由标签；移除重复节点分组/场景字段。
- 外侧提示词库增加后台提示词与五类导航，点击分类直接使用服务端 `category` 查询。

## 验证记录

- repository 分类/种子聚焦测试通过。
- repository/service/handler Prompt 聚焦测试通过。
- 提示词相关前端回归 48 / 48 通过。
- TypeScript 检查通过。
- 最终聚焦复验：前端分类/后台接线/外侧查询 5 / 5 通过，`go test ./repository ./service ./handler` 通过，`git diff --check` 通过。
- Next.js 生产构建和 Docker 镜像构建通过；`infinite-canvas-main-app-1` 容器运行状态为 healthy。
- 浏览器确认后台筛选、表格、新增弹窗均使用五类业务分类和自由标签，且不再出现重复的节点分组/场景字段。
- 浏览器确认外侧提示词库直接展示后台记录；场景分类当前读取 8 条后台提示词。当前内置记录没有视频或文本条目，因此对应分类显示空状态，后续后台新增后会直接出现。
