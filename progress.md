# 素材库专项进度

## 当前状态

- 当前阶段：7 / 已完成。
- 当前循环：验证通过，已进入 Git 发布收口。

## 已完成

- 明确专项范围、非目标和质量门。
- 保留此前素材同步、下载和重复素材修复作为当前基线。
- 运行 14 个素材相关测试文件，共 62 项用例全部通过；当前基线无失败。
- 将页面布局清洁度和画布/工作流调用闭环纳入专项范围。
- 完成素材写入入口初步盘点：媒体主干基本统一，文本工作流仍存在同步直写和仅视图去重的问题。
- 确认 Store 恢复仍可能覆盖水合前的同步写入；确认页面头部和结果工具栏存在重复计数与操作优先级问题。
- 确认跨流程消费者仍普遍读取原始重复记录；确认分页被关闭导致长列表全量渲染，结果区存在多层重复统计。
- 定位画布素材选择器和工作流素材面板两个关键调用问题；完成筛选区信息密度审计。
- 完成导入导出与 Blob 生命周期初审：版本 Blob 未随包导出、导入复用 storageKey、去重导入计数不准确。
- 完成 Store 恢复合并：恢复期间同步新增不再被持久化旧快照覆盖。
- 完成工作流文本写入去重并保留嵌套的工作流关联字段；工作流面板改用安全异步写入。
- 画布素材选择器已改用规范视图，跨流程取用不再显示同组工作流别名。
- 恢复主素材页分页，默认每页 30 条，长列表不再全量渲染。
- 素材包已收集当前及历史版本文件并去重；重复导入的返回 ID 和提示计数已归一。
- 导入包的 storageKey 已重新分配并递归回写，避免同源旧包覆盖现有 Blob。
- 完成第一轮界面清理：移除头部指标卡和搜索区重复计数，收拢高级筛选，删除项目/集数/类型的重复快捷计数层。
- 在临时数据库和隔离后端中完成真实页面验收：默认筛选层级、高级筛选展开/收起、空状态和导航均正常，浏览器控制台无错误。
- 确认画布素材选择器读取规范视图，工作流写入使用安全去重路径；跨流程不会重新暴露隐藏别名。

## 下一步

1. 由用户执行服务器更新并确认线上版本。

## 验证记录

- 素材专项基线：62 / 62 通过。
- 恢复/去重专项：13 / 13 通过。
- 导入导出专项：7 / 7 通过。
- storageKey 重映射加入后：8 / 8 通过。
- 素材专项最终回归（实现阶段）：68 / 68 通过。
- 隔离页面验收：素材页可进入；默认高级条件隐藏；展开/收起交互正常；浏览器 error 日志 0 条。
- 前端完整测试：631 / 631 通过。
- `git diff --check`：通过，无空白错误。
