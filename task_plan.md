# 素材库全面整理与优化计划

## 目标

在保留当前浏览器本地素材架构和已有用户改动的前提下，对“我的素材”完成一次正确性优先的专项整理：统一素材身份与写入路径，修复可复现的数据/交互问题，降低重复计算和误删除风险，优化页面清洁度，并验证画布/工作流调用闭环。

## 执行策略

采用增量收口，不重写素材库：

1. 先建立现状清单和验证基线。
2. 按“数据写入 → 查询视图 → 用户动作 → 持久化/刷新”追踪完整链路。
3. 每个问题先写失败测试，再做最小修复。
4. 每批修改后运行针对性测试和页面验证；失败则定位根因并进入下一轮修复。
5. 最后只对改动范围做质量门检查，不主动运行全量构建。

## 范围

- 浏览器本地素材 Store、恢复与持久化。
- 画布/视频工作流/手动导入等素材写入入口。
- 重复素材、素材版本、引用兼容与删除行为。
- 素材库计数、搜索、分组、筛选、选择、分页和导入导出。
- 图片/视频/音频的 Blob 生命周期与下载。
- 素材库页面计算性能和不必要的重复转换。
- 素材库页面层级、首屏操作密度、重复信息和长列表表现。
- 画布、项目工作台、生产包/素材拆解等来源与素材库之间的写入和回取路径。
- 相关纯函数、Store 和页面回归测试。

## 非目标

- 不把本地素材改造成云同步系统。
- 不另造脱离现有画布主题的整套视觉体系。
- 不修改无关的视频生产、后台管理或 AI 渠道逻辑。
- 不物理清理无法安全判定归属的历史素材。

## 阶段

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | 建立当前测试基线、文件/数据流、界面与跨流程调用清单 | completed |
| 2 | 审计并修复素材身份、恢复、写入和去重 | completed |
| 3 | 审计并修复查询、分组、筛选、选择和删除 | completed |
| 4 | 审计并修复导入导出、Blob、版本和引用安全 | completed |
| 5 | 收口性能与代码边界，补足测试 | completed |
| 6 | 页面与跨流程验收、验证—修复循环 | completed |
| 7 | 文档、差异和交付检查 | completed |

## 质量门

- 新增或修复行为必须经历 RED → GREEN。
- 同项目/同集的工作流重复素材只显示一个规范记录，不跨范围误合并。
- 导入、下载、导出、删除和刷新后素材数量符合预期。
- 普通同名素材、不同项目/集数素材保持独立。
- 画布生成素材不会被 Store 恢复覆盖。
- 改动文件无空白错误，针对性测试全部通过。

## 第一批修复顺序

1. Store 恢复合并，确保恢复期间的同步新增不丢失。
2. 工作流文本素材写入去重，并让画布素材选择器使用规范视图。
3. 恢复长列表分页，减少一次性渲染。
4. 素材包带齐版本文件并修正重复导入计数。
5. 压缩头部、筛选与结果区重复层级，再进行真实页面验证。

## 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 暂无 | 0 | - |
| 首次补充计划时补丁上下文与实际标题不一致 | 1 | 重新读取计划文件后按现有内容更新 |
| 首次补充 Store 测试时 import 行上下文不一致 | 1 | 读取现有测试后按实际 import 更新 |
| 本地 3000 端口已被 Docker 服务占用 | 1 | 不终止现有服务，改用 3010 启动当前代码验证 |
| 已上线实例的管理员种子密码已变更，无法用于本地自动登录 | 1 | 不写入线上数据，启动临时数据库和隔离后端完成页面验收 |
| Next 开发代理转发流式登录请求时测试缓冲代理保留了传输头 | 1 | 修正临时代理头后确认请求可通；最终改用隔离后端直连，不修改业务代码 |
| 临时后端 8081 端口已被占用 | 1 | 改用 18081，未终止已有进程 |
