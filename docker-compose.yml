services:
  app:
    build: .
    image: blink-workbench:local
    container_name: blink-workbench
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "3000:3000"
    restart: unless-stopped
