services:
  app:
    build: .
    image: blink-workbench:local
    container_name: blink-workbench
    env_file:
      - .env
    environment:
      APP_ENV: production
      DREAMINA_HOME: /app/data/dreamina-home
      DREAMINA_OUTPUT_DIR: /app/data/jimeng-cli
    volumes:
      - ./data:/app/data
    ports:
      - "3000:3000"
    restart: unless-stopped
