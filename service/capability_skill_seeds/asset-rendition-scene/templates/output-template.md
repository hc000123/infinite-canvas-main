# 图片输出模板

生成一张完整场景主参考图：

- 空间：严格遵循输入 `asset_brief.brief` 的结构、时代、材质、时间与照明。
- 构图：遵循 `asset_brief.format`，优先呈现入口、动线、功能区和关键陈设关系。
- 画面：透视可信、尺度明确、层次清楚、材质可辨，适合作为后续镜头空间基准。
- 禁止：主演、剧情动作、结构漂移、水印、标题、文字说明和分镜边框。

图片归档字段由 Runtime 根据真实生成结果写入，不要求模型输出 JSON。
