# 图片输出模板

生成一张完整角色资产参考图：

- 主体：严格遵循输入 `asset_brief.brief` 的角色身份与状态。
- 版式：遵循 `asset_brief.format`；四视图采用同一人物的统一尺度并排展示。
- 画面：全身完整、不裁切关键部位、背景简洁、光线均匀、材质清晰。
- 禁止：额外人物、剧情动作、环境叙事、水印、标题、文字说明和身份漂移。

图片归档字段由 Runtime 根据真实生成结果写入，不要求模型输出 JSON。
