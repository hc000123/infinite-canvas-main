# CHANGELOG

## Unreleased

+ [新增] 新增统一 Artifact 与 Invocation Runtime，独立 Skill 调用可冻结版本、预检契约、追踪质量门并在审核后幂等 Apply。
+ [新增] 新增可版本化 Agent Registry 与顺序 Agent Plan Runtime，支持项目自由组合独立 Skill、冻结版本和额度，并通过统一 Invocation / Artifact 链逐步审核交接。
+ [新增] 项目新增 Workflow 中心，可自由组合 Skill / Agent DAG，支持固定、标签和运行前手选路由、契约校验、版本发布、成本预览、审批、重试、取消及 Artifact 轨迹恢复。
+ [新增] 新增内容标签分类、角色 / 场景 / 道具独立资产 Brief、三类资产成图、竖屏短剧与横屏中长剧分镜 9 个可独立发布 Skill；标准生产 Workflow 升级为 12 节点 `2.1.0`，并行生成三类资产图片并按制作参数路由分镜版本。
+ [新增] 生图工作台可直接调用已发布 Skill，统一完成 Artifact 输入匹配、版本/额度冻结、人工审核、提示词或 `asset_rendition` 图片写回、本地消费回执与素材保存，并保留精确 Invocation 追溯坐标。
+ [新增] 画布节点可从悬浮工具栏或检查器直接调用已发布 Skill，批准后的文本 Artifact 与图片型 `asset_rendition` 会按类型创建带完整 Invocation 溯源的下游节点并建立连线，重放和刷新不会重复产物。
+ [新增] 画布对话可选择已发布 Agent，将目标和节点语义引用创建为可编辑 Temporary Plan，并通过统一 Agent Plan / Invocation / Artifact Runtime 逐步确认、审核和幂等写回最终产物。
+ [优化] 正式视频 Workflow 已迁入统一 Invocation / Artifact Runtime，阶段支持精确 Skill 冻结、标准资产与媒体引用、完整产物集审核、幂等 Apply 和失败安全门，并保持现有工作台接口与刷新恢复体验。
+ [优化] Invocation Worker 新增图片模型执行与内容哈希媒体归档，支持角色 / 场景 / 道具资产成图、失败重试和 Workflow 多上游 Artifact 聚合。
+ [优化] Invocation Worker 可按每个冻结任务的执行器类型分流；显式本地 Codex 开发模式下，文本节点使用 Codex CLI，图片节点仍使用配置的图片 API，生产配置继续保持 API-only。
+ [优化] 将固定六阶段 Workflow Skill 中心泛化为可搜索、可版本化、可评测和可独立管理的 Skill Registry。
+ [优化] 移除画布助手旧硬编码 Prompt Agent Skill Pack、工具注册表和独立执行状态；生产 Agent 统一来自 Agent Registry，同时保留普通对话和直接生图。
+ [优化] 项目分集剧本优化改用 Registry 推荐版系统 Agent，通过统一 Artifact / Agent Plan / Invocation 审核链运行，并移除旧项目 Workflow preset 选择状态、脚本直调 Runner 与重复 Agent 设置面板。
+ [修复] 修复 Workflow 新建时空 Skill 契约导致页面崩溃、默认选中不兼容 Skill、未保存草稿可误校验和编辑器弃用警告。
+ [修复] 修复 Invocation 成功预检返回空集合时画布 Skill 抽屉读取 `null` 导致页面崩溃的问题，并统一空集合安全响应。
+ [修复] 修复安全升级后的 `brace-expansion@5` 与 ESLint 传递依赖 `minimatch@3` API 不兼容导致全量 ESLint 崩溃的问题；保留安全版本并通过可复现的 Bun 依赖补丁兼容旧调用方式。
+ [验证] Go 全包、前端 777 项测试、TypeScript 与 Next.js 生产构建通过；隔离浏览器完成资产成图、审核、Apply、生图工作台展示 / 保存和画布图片节点写回，测试图片仅用于协议验收，正式视觉质量待真实图片模型复测。
+ [验证] 固定公交站剧本的 12 节点生产 Workflow 已通过模拟混合执行验收：9 个文本节点分流至模拟 Codex、3 个成图节点分流至模拟图片 API，并完成版本冻结、Artifact 血缘、质量门、审核、计费、交付报告和刷新恢复；真实视觉效果保留人工验收。
+ [调整] 移除项目详情低频“素材引用”页签，素材缺口与旧版本提醒统一进入项目素材库，底层引用和生成链路保持不变。
+ [优化] 模型算力点改为按业务单位计费：语言按调用次数、图片按实际张数、视频按生成秒数，并统一画布预估、后台单位提示和后端扣费口径。
+ [优化] 六阶段 Workflow Skill 升级为 `3.0.1` 生产多文件包，完整加载领域规则、输出模板与示例，并在冻结任务中强制必需输入、参考图策略和 JSON Schema 质量门；已有官方内置绑定会新增并切换版本，自定义全局绑定保持不变。

## v0.2.97 - 2026-07-25

+ [新增] 管理后台新增超级管理员层级、管理员管理、用户独立用量详情、低频操作审计和白名单外登录审批，并强化受限账号会话与登录 IP 绑定。
+ [新增] 素材库支持按创作项目、视频工作流和子画布分层筛选，补齐项目分页、稳定类型分组、自然排序、收藏及图片 / 视频紧凑卡片展示。
+ [新增] 画布可预览并快速断开上游媒体，复制节点继承上游连线，同时清理旧任务结果，便于从相同输入重新生成。
+ [优化] 画布提示词自动保存并保留结构化媒体引用顺序；视频刷新恢复、版本下载命名和参考素材提交顺序保持一致。
+ [优化] 大画布增加节点、连线与缓存容量提示，复用生成输入索引、裁剪视口外连线，并避免高密度节点重叠放置。
+ [修复] 修复即梦 CLI 参考素材提交、素材项目筛选不完整以及重试状态测试中的不安全可选链断言；升级 Next.js、Axios、PostCSS 与 Sharp，关闭上线审计发现的直接运行时高危漏洞。
+ [验证] Go 全包测试、前端 740 项测试、TypeScript、ESLint、Next.js 生产构建和 Docker 生产镜像验收通过；独立容器健康检查、核心页面、数据挂载及生产镜像 CLI 门禁正常。

## v0.2.96 - 2026-07-24

+ [新增] 项目原剧本支持直接编辑；资产提取与分镜拆解可从确认稿并行运行，并在每次任务中选择、冻结后台已发布 Skill 版本。
+ [新增] 资产卡支持从本地或素材库导入图片并绑定稳定资产编号、保留版本历史；有效分镜通过质量门后自动载入可编辑镜头队列。
+ [优化] 参考图改为逐镜绑定，支持角色、角色马甲、场景、道具、站位和连续性用途；移除冗余镜头摘要与提示词保存按钮，改为自动保存后确认。
+ [新增] 视频工作流收为“资产设计 + 镜头生产”两阶段生产台，打通稳定资产编号、生图工作台回写、原剧本对应的可编辑分镜、逐镜确认和视频生成。
+ [新增] 单镜提示词结合实际参考图片、原剧本和已确认分镜生成，冻结逻辑资产 ID、素材库 ID、版本、用途与 `@图N` 顺序，并以输入哈希阻止过期结果写入。
+ [新增] 视频成功后归档尾帧；同场景连续下一镜自动将其绑定为剧情连续性普通参考图，不作为首帧，不要求复刻画面，也不静默替换已冻结版本。
+ [优化] 资产提取、资产生图提示词、分镜拆解和镜头提示词保留四个可独立发布 / 灰度 / 回滚的后台 Skill，内置合同升级到 `2.0.1`。
+ [新增] 视频工作流支持本地 Codex CLI 多模态验证、一次性参考图批次和可审核的图片理解证据；生产环境硬性固定企业 API 且镜像不包含 Codex。
+ [新增] 管理后台新增六阶段 Skill 版本中心，支持契约校验、dry-run、同输入对比、项目灰度、全局推广、回滚和审计追踪。
+ [优化] 统一页面展示执行器、冻结 Skill 版本 / 哈希、参考图选择和逐镜头视频参数；付费确认统一展示企业通道实际模型、时长、清晰度与画幅。
+ [修复] 修复 CLI 个人模型配置冲突、数组型图片证据误判、空队列日志噪声、历史阶段尝试覆盖最新状态、依赖解除后仍阻断及英文内部状态泄漏。
+ [修复] 资产准备拆为独立 Agent 阶段，按环境真实调用 Codex CLI 或企业 API，并补齐资产 Skill 冻结、质量门、审核、写入回执及分镜阶段依赖。
+ [修复] 统一 Stage 3 分镜质量门与视频生成提示词合同，阻止只含电影感摘要的分镜通过审核后才在付费前被拦截。
+ [优化] 视频阶段在尚无生产包时也可提前执行不扣费的企业通道预检，便于上线前排除密钥、模型和端点配置问题。
+ [验证] 完成一次且仅一次企业 Ark 最低消耗视频冒烟：`doubao-seedance-2-0`、4 秒、720P、16:9，任务成功回填 V1 并归档到我的素材，扣费 300 算力点。

## v0.2.95 - 2026-07-19

+ [修复] 画布复制粘贴节点自动错开放置，重置视图按真实画布尺寸适配全部节点，保存视图刷新后不再回到 100%，并允许连接上游文本的生成节点直接使用引用提示词。
+ [修复] 用户工作区新增统一登录门禁和按账号隔离的项目、画布、素材、配置、任务、生成记录及媒体文件存储；登录后会回到原页面，损坏图片会明确报错且不写入画布。
+ [验证] 完成画布真实界面全流程、300 节点保存与 Docker 重启恢复、双账号隔离、无效素材恢复、星链云最低时长视频回填，以及健康接口和登录页各 200 次并发压力测试。

## v0.2.94 - 2026-07-19

+ [新增] 画布助手升级为提示词 Agent，支持图片 / 视频 / 分镜提示词卡片、工作流 Skill Pack 选择、问答 / 自动 / 审核模式、助手工具箱、执行计划卡、工具权限展示和执行状态回写；确认后可创建对应画布节点，并允许图片类确认后走生图链路。
+ [新增] 即梦 CLI 视频渠道开放文生、单图、首尾帧、多帧和全能参考五种模式，支持素材安全暂存、任务账本、查询下载与失败退款。
+ [新增] 接入星链云 `xinglian-cloud` 独立视频协议，覆盖 9 个 SD2 720p / 1080p 模型、文生视频、图生视频、任务查询和结果回写。
+ [新增] 管理后台新增火山 Ark、星链云、即梦 CLI、Comfly 和 OpenAI 兼容渠道的一键厂商预设，可一次写入协议、能力与模型，并安全保留已有密钥。
+ [调整] 上线版本移除本地 Codex CLI / Runner 执行入口，原视频工作流统一显示“云端执行器尚未启用”，相关接口禁止回退本机执行。
+ [修复] 统一文本、图片、视频模型目录与节点解析顺序，协议只以后端模型映射为准，并阻止同名模型跨协议配置造成调用错配。
+ [验证] 完成 Go、前端、TypeScript、Next 生产构建和 Docker 独立容器验收，覆盖健康检查、持久化、公开素材路径、执行器门禁及内置官方 Dreamina CLI 固定哈希校验。

## v0.2.93 - 2026-06-17

+ [修复] 前后台模型选择按文本 / 图片 / 视频分桶展示同类全渠道模型，移除后台通用“默认模型”入口，并将本地 Codex CLI 改为默认关闭的独立开关，避免执行方式切换影响火山 Ark 与 OpenAI 兼容视频模型可见性。

## v0.2.92 - 2026-06-17

+ [优化] 拆分视频生产台页面结构，保留逐条视频生成、任务同步和详情抽屉能力，降低页面维护成本。
+ [修复] 画布视频模型选择按后台模型协议映射识别火山 Ark 与 OpenAI 兼容渠道，避免 Seedance 参考素材走错请求格式。
+ [修复] 收口画布生图默认并发、生成图片节点标题和参考图上传文件名，降低视频生成因文件名过长失败的风险。
+ [调整] 移除空画布文生图预设启动器，修复顶部导航压缩滚动和后台渠道模型选择冗余手动输入入口。
+ [验证] 复跑 Docker 上线前验收，确认生产镜像构建、健康检查、持久化、公开素材代理路径和进程联动通过。

## v0.2.91 - 2026-06-11

+ [修复] 修正 Seedance 首尾帧和多模态素材引用参数，避免图片内容缺少 role 导致任务创建失败。
+ [优化] 拉长火山视频任务、素材审核和代理链路的长请求等待时间，减少图生视频与加白流程在云服务器上的超时。
+ [优化] 完善素材上传、画布粘贴和剧集工作台素材模块的异常提示与待测记录。

## v0.2.90 - 2026-06-11

+ [新增] 画布助手支持根据工作流待落地视频配置生成可确认动作预览，用户确认前不会写入画布或触发生成。
+ [优化] 工作流落地页在阶段未生成、待确认或阻塞时禁用“生成预览”，并展示明确原因，降低误操作风险。

## v0.2.3 - 2026-06-03

+ [修复] 修复版本检查地址硬编码问题，避免部署到自有仓库后仍读取旧上游版本。
+ [修复] 视频设置面板按视频协议识别 Seedance，避免 OpenAI 秒数和 Ark 参数混在同一个界面中展示。

## v0.2.2 - 2026-06-03

+ [安全] 加固云端/本地模式边界：商业云端关闭自定义渠道时，前端不再展示或使用本地供应商 Key，云端代理忽略前端传入的火山 Key，并仅使用后端私有渠道配置。
+ [调整] 品牌展示更新为“眨眼之间”，同步 Web、桌面端、导航、首页、登录页、画布默认命名和导出文件名。
+ [调整] 提示词管理移除内置远程提示词源，后端只保留管理员手动维护的系统提示词，并在启动时清理旧内置分类数据。
+ [优化] 收口 Seedance 视频设置、火山素材审核配置和我的素材操作布局，减少重复配置入口并改善窄屏展示。
+ [新增] 我的素材支持本地文件夹管理和直接拖拽上传图片、视频、音频及素材压缩包。

## v0.1.0 - 2026-05-26

+ [优化] 优化我的画布、我的素材导出功能
+ [修复] 修复画布撤销，配置节点等bug问题

## v0.0.9 - 2026-05-26

+ [新增] 新增视频创作台页面。
+ [修复] 修复图片节点size参数传递问题。

## v0.0.8 - 2026-05-24

+ [新增] 新增用户账号与算力点体系，支持账号密码注册登录、Linux.do OAuth。
+ [新增] 管理后台公开配置支持设置模型算力点、支持计费查询。
+ [新增] 画布右上角展示用户算力点余额，生成按钮会展示本次预计消耗算力点。
+ [新增] 新增视频生成节点。

## v0.0.7 - 2026-05-23

+ [新增] 管理后台提示词管理支持多选批量删除。
+ [新增] 新增定义拉取GitHub提示词源功能。
+ [新增] 新增awesome-gpt-image2-prompts提示词来源。
+ [优化] 优化模型下拉选择样式、优化生图编辑设置

## v0.0.6 - 2026-05-22

+ [新增] 管理后台支持配置模型渠道，前端当前无需鉴权即可直接使用后端渠道能力。
+ [优化] 统一整理后端错误提示、AI 代理、图片节点生成与重试、参考图缺失处理等细节。
+ [优化] 后端模型代理路径调整为 OpenAI 风格。

## v0.0.5 - 2026-05-20

+ [新增] 右上角版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和按时间线整理的更新日志。
+ [新增] 设置弹窗支持配置系统提示词，AI 生图、编辑图和文本请求会自动携带。

## v0.0.4 - 2026-05-20

+ [调整] Docker 运行入口改为 Next.js 对外提供页面，`/api/*` 由 Next.js 代理到内部 Go 服务。
+ [修复] 文本复制在局域网 IP 访问时可能失败的问题。

## v0.0.3 - 2026-05-19

+ [修复] 更新 nanoid 依赖并修改 ID 生成方式，防止其他ip无法使用crypto模块导致的ID生成失败问题。

## v0.0.2 - 2026-05-19

+ [新增] 增加生图工作台功能，支持文生图、图生图、查看历史记录，并增加移动端适配。
+ [修复] 画布生成尺寸控件支持选择更多常用比例，并可直接输入自定义比例。
+ [修复] 生成配置节点恢复拖拽操作，避免面板控件拦截整块节点拖动。
+ [文档] 增加 Render 部署说明。

## v0.0.1 - 2026-05-19

+ [新增] 首次开源版本，包含无限画布能力：多画布项目、节点拖拽缩放、连线、小地图、撤销重做、导入导出。
+ [新增] AI 创作能力：支持 OpenAI 兼容接口的文生图、图生图、参考图编辑和文本问答。
+ [新增] 画布助手能力：支持围绕选中节点和上游节点对话、生图，并把结果插回画布。
+ [新增] 提示词库能力：抓取多个 GitHub 开源项目，按案例整理数百个图片提示词。
